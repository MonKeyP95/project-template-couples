# TODO.md

## Current Phase
**Phases 1–4.6 complete; itinerary work (locations, spans, events, during-trip mode, home today signal) all shipped through 2026-06-10. Next is Phase 5 — AI assistant** (Anthropic Claude via `lib/ai/claude.ts`, not yet wired; the moss-bordered `SuggestionCard` is its placeholder). The completed-task log below stays as history.

**Real budget planner: shipped 2026-07-09 (build-clean + seam live-verified; full in-app round-trip pending a logged-in session).** The second mock-to-real AI swap after chat — the guided budget drafter now drafts real, destination-and-trip-aware line items instead of flat multipliers. Principle: **deterministic scaffold, AI content** — the five category steps + location grouping stay in the pure `planBudgetSteps` (that's UI), and only the seed line items (subject/when/amount) come from Claude. New `draftBudgetSeeds` seam in `claude.ts` (own `BudgetDraftContext`/`DraftedBudgetItem`; plain `messages.create` + a **forced** `propose_budget` structured tool; **no web_search** — a budget is an estimate and parametric knowledge answers in one round-trip, avoiding the discovery latency tax). New `"use server"` `draftBudget` action (`src/lib/ai/budget-actions.ts`) builds the scaffold, loads destination/vibe/brief (`getTripBySlug`) + budget band (`getDiningPreferences`) server-side, calls the seam, and **merges** items into buckets by matching the echoed location name to a group (case-insensitive; unmatched grouped items dropped, flat categories place-agnostic). The client `BudgetDrafter.open()` is now **async**: Plan-a-budget / Start-over await `draftBudget` (button shows "drafting…"), Edit-budget (saved rows exist) stays local with **no AI call**; on model failure the interview still opens on the mock scaffold with a clay "couldn't reach the assistant — using rough estimates" note (`drafted:false`). `estimateItemCents` stays deterministic (blank user rows). Suggest-only (Apply's `saveBudgetItems` is the only write), AI-gated (unchanged), no migration/deps/vendor. Seam proven live via a throwaway smoke route (Morocco/2 pax/3 places -> 20 items, exact location names, budget-brief-aware amounts), then removed + re-gated. Deferred: real per-item estimate, web-grounded live prices, reading logged expenses to refine. Spec: `docs/superpowers/specs/2026-07-09-budget-planner-real-design.md`. Plan: `docs/superpowers/plans/2026-07-09-budget-planner-real.md`.

**Real trip chat (slice 7): shipped 2026-07-08.** First mock-to-real AI swap. The floating `Assistant` chat now calls a real `claude-sonnet-5` model instead of the deterministic placeholder. The call moved server-side: new `chatReply` in `lib/ai/claude.ts` (own `CHAT_MODEL` constant, separate from discovery's `MODEL`), reached via a new `"use server"` action `sendChatMessage` (`src/lib/ai/chat-actions.ts`) that assembles basic trip context (name/dates/country/itinerary places + a planning-vs-on-the-road hint) from `getTripBySlug` + `getItineraryLocations` when open on a `/trips/[slug]` page. `ChatMessage` moved to `src/lib/ai/chat-types.ts` (client-safe); mock `chat.ts` deleted. Not gated by the AI toggle (chat always works). Non-streaming; deep context (budget/packing/notes), streaming, and saved history deferred. Spec: `docs/superpowers/specs/2026-07-08-real-trip-chat-design.md`. Plan: `docs/superpowers/plans/2026-07-08-real-trip-chat.md`.

**Restaurant discovery agent (Phase 5) — designed 2026-06-29, not yet built.** A preference-aware, on-demand restaurant finder: with AI mode on, ask the Assistant for a place, Claude runs a grounded web search (its built-in `web_search` tool, no new vendor or key) seeded by the couple's stored tastes, and returns a cited shortlist; you book off-app and confirm the pick into the itinerary as an event; a good/bad rating is captured for later learning. Suggest-only, restaurants-first. Rules out scraping/agent-driving Booking.com (ToS + anti-bot + stored-credential risk) in favor of open web search + our own preference data. Spec: `docs/superpowers/specs/2026-06-29-restaurant-discovery-design.md`. Slices: **A** preferences -> **B** discovery loop -> **C** accept-to-event -> **D** feedback. Plan for slice A (dining-preferences table + `/profile` editor): `docs/superpowers/plans/2026-06-29-restaurant-discovery-slice-a-preferences.md` (**shipped 2026-06-29**). Plan for slice B1 (real web-search discovery seam + curl-testable `/api/ai/discover` smoke route; B2 = Assistant UI follows): `docs/superpowers/plans/2026-06-29-restaurant-discovery-slice-b1-discovery-seam.md`.

**Two-modes principle documented: 2026-06-29.** Planning vs. on-the-road is now a product-wide principle — every feature is designed for both, mode is dates-driven (today within the trip = on-the-road), not a manual toggle. Recorded in `docs/VISION.md` (the "Two Modes" section) and `docs/DECISIONS.md` (2026-06-29 row, with the why). The standing design question for any new slice: "what does this look like in planning vs on the road?" First applied to restaurant discovery (one engine, two front doors).

**Discovery slice D (feedback capture): shipped 2026-07-07 (live-verified).** A 1-5 rating + optional note on any **past** event, store-only (feeding it into discovery ranking is deferred — needs the learning layer). Broadened from restaurants to any event: a quiet vs busy beach day is signal too. `ItineraryEvent` gained optional `rating?` (1-5) and `note?` (no migration — jsonb; `parseEvents` reads them only when valid). New `rateEvent` action writes them onto one event, addressed by its index in the day's `sortDayEvents`-sorted array (the action sorts the same way every surface renders). Shared `EventRating` editor (`src/components/event-rating.tsx`): collapsed shows the stars or `☆ rate`; tap expands a 5-star picker + note + save/clear/cancel. Two surfaces (the two-modes split): itinerary past days (`day.dayDate < today` — `today` threaded through DaySegmentView -> DayCard -> DayView) for review, and a new on-the-road **"Looking back"** list (`src/app/on-the-road/today-past.tsx`) of today's already-passed events (`e.time < now`, client clock) for rating in the moment. Invariant honored: rating/note are preserved through every event-array rewrite (`addTodayEvent` merge; the `DayForm` editor via `EventDraft` pass-through + submit maps) so an edit never wipes a rating. Not in the planning editor's visible fields; not in the public `/t/` projection. Deferred: per-partner ratings, note inline on cards, using ratings to bias search. Spec: `docs/superpowers/specs/2026-07-07-restaurant-discovery-slice-d-feedback-design.md`. Plan: `docs/superpowers/plans/2026-07-07-restaurant-discovery-slice-d-feedback.md`. Verified in-app (rated on both surfaces; edit preserves the rating) 2026-07-07.

**Discovery slice C (accept-to-event): shipped 2026-07-07 (live-verified).** An accepted pick is now a keep-worthy itinerary event carrying its source link and an optional date/time. `ItineraryEvent` gained an optional `url` (no migration — `events` is jsonb; `parseEvents` reads it only when non-empty). The link is a plain, hand-editable itinerary capability: the normal day editor (`DayForm` in `itinerary-tab.tsx`) has a per-event "link (optional)" input, and day cards render `↗ source` after an event's text when present (works with AI off). Both discovery doors changed accept from immediate to a small inline confirm: on-the-road (`find-a-place.tsx`) adds an optional time (date stays today); planning (`find-a-place-planning.tsx`) adds a day-picker of the browsed location's existing days (default earliest) + optional time — a day-picker, not a free date, so the pick always stays filed under the location. Only `addTodayEvent` changed in the action layer (optional `url`); both doors route through it. Deferred: link in the public `/t/` projection, URL validation, Slice D feedback. Spec: `docs/superpowers/specs/2026-07-07-restaurant-discovery-slice-c-accept-to-event-design.md`. Plan: `docs/superpowers/plans/2026-07-07-restaurant-discovery-slice-c-accept-to-event.md`. Verified in-app (accept in both doors) 2026-07-07.

**Discovery slice B2 (auth'd preference-aware route): built 2026-06-29 (build-clean; live unverified).** `/api/ai/discover` is now the real endpoint, not the smoke route: keeps the AI-mode 403, resolves the workspace via `getCurrentWorkspace()` (401 if no session), loads `getDiningPreferences(workspace.id)` (slice A), and merges those into the `RestaurantQuery` built from the body's `{destination, when}`. Preferences are server-authoritative; the doors are unchanged (still send `{destination, when}`, now get preference-aware results). Decision: preferences-only enrichment, no `tripId` (the doors already know destination/when more precisely than a trip row). Spec §7 + plan: `docs/superpowers/plans/2026-06-29-discovery-slice-b2-authd-route.md`. Live verification (preference shift) needs a logged-in session — the unauthenticated curl path is gone by design.

**Discovery planning door (slice B-planning): built 2026-06-29 (build-clean; live UI unverified).** The planning-mode twin of the on-the-road door, on the **itinerary tab**. New `src/app/trips/[slug]/find-a-place-planning.tsx` (`FindAPlacePlanning`) mounts in the itinerary planning block (renders only when `!active`, so on-the-road owns active trips). A location `<select>` (defaults to first) + "find dinner" POSTs `/api/ai/discover` with `{destination: location name, when: "dinner"}` (preference-aware via B2), renders 3-4 cited rows (moss), and "add to <location>" appends `"Dinner · <name>"` to that location's earliest day via the existing `addTodayEvent` (disabled with "add a day first" when the location has no days). **Reverses spec §3's original "Assistant chip" sketch** — the Assistant is a context-free global mock; the itinerary tab already holds trip context and mirrors the on-the-road on-page pattern (decided with the user 2026-06-29). Spec §3 + plan: `docs/superpowers/plans/2026-06-29-discovery-slice-b-planning-door.md`. Open items flagged for review: native `<select>` styling (project has a custom Select primitive), and meal is hardcoded "dinner" (no meal picker yet). Live verification needs a logged-in session.

**Discovery on-the-road door (slice B-road): shipped 2026-06-29.** The first on-the-road front door for restaurant discovery, applying the planning-vs-on-the-road principle (`docs/VISION.md`): one engine, two doors. A small AI-mode-gated block on `/on-the-road` (`src/app/on-the-road/find-a-place.tsx`) infers the current meal from the device clock (`meal-slot.ts`: `<11` breakfast, `<16` lunch, else dinner; via `useSyncExternalStore` so SSR stays null and there's no hydration mismatch), and shows **only when today's events have no title containing that meal word** (keyword heuristic). On tap it POSTs the existing smoke `/api/ai/discover` with `{ destination: today's location (fallback country/name), when: "<meal> tonight/today" }`, renders 3-4 cited rows (moss accent, name · why · area · price · "source — verify hours"), and **one-tap "add to today"** drops `"<Meal> · <Name>"` onto today's events via the existing `addTodayEvent` (no confirm; editable after). Built on the body-driven smoke route with **no preferences yet** — when slice B2 makes the route auth'd + preference-loading, this door needs no change (same path/body). Two documented deviations from the spec: reused the engine's free-text `when`/`destination` instead of new `targetMeal`/`nearLocationName` fields; source URL stays in the suggestion row, not on the event (`ItineraryEvent` has no note field). Spec §6 + plan: `docs/superpowers/specs/2026-06-29-restaurant-discovery-design.md`, `docs/superpowers/plans/2026-06-29-discovery-on-the-road-door.md`. lint + build clean; live browser verification still pending (needs an active trip + `ANTHROPIC_API_KEY`).

**Discovery open follow-ups (from the 2026-06-29 live API test):**
- **Re-gated before merge (resolved 2026-06-29).** `/api/ai/discover` was briefly allow-listed in `src/proxy.ts` `PUBLIC_ROUTES` to curl-test the web-search call; that entry was removed before merging to `main`, so the endpoint is auth-gated again (the proxy redirects unauthenticated requests to `/signin`). The on-the-road door calls it from an authenticated page, so the feature is unaffected. Slice B2 builds the real preference-loading endpoint.
- **Discovery latency: fixed 2026-07-07.** Root cause: `claude.ts` used the `web_search_20260209` tool variant, whose built-in dynamic filtering spins up server-side `code_execution` to pre-filter results. A timed A/B on the same bare query (Funchal, dinner, `max_uses:3`, sonnet-4-6) measured `web_search_20260209` at **176.7s** (code_execution ran, 3 search rounds) vs the basic `web_search_20250305` at **35.8s** (no code_execution, 1 round) — both returned 4 valid suggestions. Fix was a one-line swap to `web_search_20250305`. See `docs/DECISIONS.md` (2026-07-07 row).

**Two-level profile — vision brainstormed 2026-07-07 (supersedes the old paused per-trip-profile idea).** Two layers the AI reads together: a durable **couple profile** (workspace-level; the `/profile` page promoted into nav; manual dining/activities base + a later visible learned layer from Slice D ratings) and a per-trip **trip profile** (the trip's Notes tab becomes a "Profile" tab: headline + chips + free brief, notes below). Vision doc: `docs/superpowers/specs/2026-07-07-two-level-profile-vision.md`. Roadmap: (1) Trip Profile tab [built below], (2) couple profile in nav + activities, (3) AI reads the trip profile in discovery, (4) learning layer.

**Two-level profile Slice 6 (learning layer): built 2026-07-08 (migration applied 2026-07-08; in-app verification pending).** The final roadmap item: Slice D ratings become a durable corpus and a living, editable per-category summary the AI reads. Two new tables (migration `20260708000001_learning_layer.sql`): **`event_ratings`** (append-only corpus — every real 1-5 rating logs a row keyed on `workspace_id`, so it survives editing/deleting the event/day/trip; best-effort `category` tag via a meal-word heuristic; `rateEvent` writes it alongside the existing event-jsonb, unchanged) and **`couple_summaries`** (one editable markdown row per `(workspace_id, category)` — `summary_md` + `rating_count_at_generation`). New `summarizeTaste` seam in `claude.ts` (plain `messages.create`, no web_search) **evolves** the current summary — hand-edits included — rather than replacing it, so auto-replace is always safe; suggest-only + AI-gated. Staleness is **percentage-based**: stale when there's no summary yet or `(total − count_at_generation)/total ≥ 20%`, floor 3 ratings — early ratings weigh more. The couple profile's Food + Activities sections gain an editable **"What we've learned"** block (`learned-summary.tsx`): hidden below the 3-rating floor; Save (no AI) persists the edit without clearing staleness; on load with AI on + stale it background-regenerates once (ref-guarded) and swaps in. AI-toggle **"position 3"**: logging is AI-free, generation is AI-gated, the toggle itself never generates, regen happens on the next profile load; **discovery reads the summary only, never generates** (`discoveryPrompt` renders a learned line ranked after in-the-moment craving/this-trip but above the static structured base). One controller fix during build: `refreshCoupleSummary` returns a real read error instead of a false "No ratings yet." No new deps/vendor; `lib/ai` stays the one seam, suggest-only. Deferred: per-partner learning, Accommodation/Transport learning (no rating source yet), dedup of re-rates, surfacing save/refresh errors in the UI. Spec: `docs/superpowers/specs/2026-07-08-learning-layer-slice6-design.md`. Plan: `docs/superpowers/plans/2026-07-08-learning-layer-slice6.md`. Subagent-driven (8 tasks, each lint+build clean; every code task spec+quality reviewed). **Migration applied 2026-07-08; in-app smoke still pending.**

**Two-level profile Slice 5 (discovery per category): shipped 2026-07-08 (live-verified, deployed to prod).** Each mode now hosts **one four-section discovery door** whose sections mirror the profile spine — **Food** and **Activities** are live; **Accommodation** and **Transport** are visible-but-inactive "coming soon" sections (they get a door once they have a profile section, the pairing rule). The discovery engine is generalized from Food-only to a `category` param: `restaurant-discovery-types.ts` -> `discovery-types.ts` (`DiscoveryQuery`/`DiscoverySuggestion` + `DiscoveryCategory = "food" | "activity"`); `claude.ts` parameterizes the system-prompt noun and branches `discoveryPrompt` (activity drops budget/cuisines/dietary, keeps activities+vibe+trip block, no meal `when`); the structured tool is renamed `propose_restaurants` -> `propose_places`; `searchRestaurants` -> `discover`. Food behavior is byte-identical (only the tool rename + neutral "invent a place" wording changed). The route (`/api/ai/discover`) reads `category` (defaults `"food"`, back-compatible). The door body was extracted into one shared client component `src/components/discovery-section.tsx` (`DiscoverySection` — craving/near/walkable inputs + the fetch + results + add affordance; an `AddTarget` union for a fixed today-day vs a selectable day list; `buildEventText`/`ctaLabel` props), and the profile accordion was relocated to a shared `src/components/category-section.tsx` (`CategorySection`, was `ProfileCategory`). Both doors shrank to thin accordions: **on-the-road** (`find-a-place.tsx`) — Food (meal-aware `when`, today fixed day, walkable-on default) + Activities, and it **no longer hides when the meal is already planned** (dropped `todayEventTexts` + `mealAlreadyPlanned`); **planning** (`find-a-place-planning.tsx`) — a location `<select>` drives all sections, each `DiscoverySection` keyed by `location.id` so switching location remounts fresh, adds to one of the location's days. No schema/deps/AI-vendor changes; `lib/ai` stays the one seam, suggest-only. Deferred: meal picker within Food; activating Accommodation/Transport (need their profile sections first); the learning layer (slice 6). Spec: `docs/superpowers/specs/2026-07-07-discovery-per-category-slice5-design.md`. Plan: `docs/superpowers/plans/2026-07-07-discovery-per-category-slice5.md`. Subagent-driven (5 tasks + a dead-code cleanup, each lint+build clean; every task spec+quality reviewed; opus whole-branch review clean — activity path traced end-to-end); merged to main; **live-verified + deployed to prod 2026-07-08.**

**Two-level profile Slice 4 (couple profile on the category spine): shipped 2026-07-07 (live-verified, deployed to prod).** The `/profile` couple-taste zone is reorganized from one flat dining form into four collapsible category sections on the app spine: **Food** (budget/vibe/dietary/cuisines) and **Activities** (activities) are populated; **Accommodation** and **Transport** are empty labelled homes (label-only copy, no fields, an `empty` collapsed hint) until slice 5 gives them a discovery door. New client accordion `src/app/profile/profile-category.tsx` (`ProfileCategory({ title, hint?, defaultOpen?, children })`, `▾`/`▸` toggle, Food `defaultOpen`) — Manual's `Section` didn't fit (mandatory prose paragraph, toggle reveals detail rather than collapsing the body, no per-section default). Per-section save: `saveDiningPreferences` split into `saveFoodPreferences` (budget/vibe/dietary/cuisines) + `saveActivities` (activities), each partial-upserting the same `dining_preferences` row keyed on `workspace_id` — on conflict Supabase updates only the payload columns, so saving one section never clobbers the other. Account zone (display name, email/member-since `en-GB`, AI toggle) unchanged; no schema/deps/AI changes; `getDiningPreferences`/`DiningPreferences`/`parsePreferenceList`/`normalizeBudgetBand` untouched. Spec: `docs/superpowers/specs/2026-07-07-couple-profile-categories-slice4-design.md`. Plan: `docs/superpowers/plans/2026-07-07-couple-profile-categories-slice4.md`. Subagent-driven (3 tasks, each lint+build clean; task 3 reviewer: spec compliant + quality approved); merged to main; **live-verified + deployed to prod 2026-07-07.**

**Two-level profile Slice 3 (discovery reads trip profile + in-the-moment): shipped 2026-07-07 (live-verified, deployed to prod).** Live check confirmed results shift with the craving/trip-profile signals (planning door, AI mode on). Restaurant discovery now reads the **trip profile** (vibe + brief) and the couple's slice-2 **activities**, plus two search-time inputs that matter most on the road: a free-text **craving** ("what do you feel like?") and a **walkable-from-anchor** proximity constraint. A new `getTripProfile(tripId)` (`src/lib/trips/queries.ts`, RLS-scoped, tolerant `parseTripProfile`) loads the profile by id (the route knows the id, not the slug). `RestaurantQuery` gained `activities`, `trip: { vibe, brief }`, `craving`, `near`, `walkable`; the route (`/api/ai/discover`) loads couple prefs + trip profile server-side and populates them (`tripId` optional — a missing one falls back to `EMPTY_TRIP_PROFILE`). Precedence is **prompt-level, not code merge**: `DISCOVERY_SYSTEM` weights craving > this trip > couple defaults and treats walkability as a hard constraint; `discoveryPrompt` renders a craving line, a walkable/near line (`near || destination` anchor), a "The couple generally —" block (budget/vibe/dietary/cuisines/activities) and a "This trip —" block (trip vibe + brief). Both doors gained the three inputs with mode defaults: **on-the-road** (`find-a-place.tsx`) prefills `near` from today's location and defaults **walkable on** (you're on foot); **planning** (`find-a-place-planning.tsx`) prefills `near` from the selected location (resets on change) and defaults **walkable off**. All inputs optional — an untouched search behaves like before. No migration, no deps, no new vendor (proximity is text the model searches on, not a geo radius); `lib/ai` stays the one seam, suggest-only. Deferred: past ratings into ranking (slice 4), who/headline, a meal picker, discovery for other categories. Spec: `docs/superpowers/specs/2026-07-07-discovery-reads-trip-profile-slice3-design.md`. Plan: `docs/superpowers/plans/2026-07-07-discovery-reads-trip-profile-slice3.md`. lint + build clean on every task; **live-verified 2026-07-07** (a real preference-shift search in a logged-in session with AI mode on — craving change swung the picks).

**Two-level profile Slice 2 (couple profile in nav + activities): built 2026-07-07 (build-clean; in-app check pending).** The `/profile` page is now a first-class nav destination, "Couple profile". `dining_preferences` gained an `activities text[]` column (migration `20260707000002_dining_activities.sql`, applied) plumbed through `DiningPreferences` / `getDiningPreferences` / `saveDiningPreferences`; a free-text **Activities you love** input joins the existing budget/vibe/dietary/cuisines form (same `parsePreferenceList` shape, not a chip set — see `docs/DECISIONS.md` 2026-07-07 row). `app-nav.tsx` gained a `"profile"` `NavKey`, a Profile destination in `buildNavDestinations` (after Manual, so it rides every shell page's `LeftRail`), and `"profile"` at the end of `MOBILE_NAV_ORDER`. `/profile` was reshaped from a standalone centered card into the standard app shell (`LeftRail` + `MobileHeaderNav`, mirroring `manual/page.tsx`) with a workspace guard; the in-body Appearance toggle and "Back to home" link were dropped (the rail carries them). Manual only — `activities` is persisted but not yet read by the AI (the discovery route maps only budget/vibe/dietary/cuisines into `RestaurantQuery`); wiring it into discovery is slice 3. Spec: `docs/superpowers/specs/2026-07-07-couple-profile-nav-activities-slice2-design.md`. Plan: `docs/superpowers/plans/2026-07-07-couple-profile-nav-activities-slice2.md`. Migration applied; lint + build clean; in-app verification pending.

**Two-level profile Slice 1 (Trip Profile tab): shipped 2026-07-07 (live-verified).** The trip's Notes tab is now a **Profile** tab. A `trip_profile` jsonb column on `trips` (migration `20260707000001_trip_profile.sql`, applied) holds `{ headline, vibe[], who, brief }`; a tolerant `parseTripProfile` + fixed vibe/who constants live in `trip-profile-types.ts`. `ProfileTab` (`src/app/trips/[slug]/profile-tab.tsx`) renders the form top-to-bottom — headline, **About this trip** (free brief, at the top), **Categories**, **Vibe**/**Who's coming** (fixed chips), one **Save** (`saveTripProfile`) — then the existing `<NotesTab>` reused unchanged below. **Categories = the trip's shared `expense_categories`** (same list as Budget; add/remove via the existing `addExpenseCategory`/`deleteExpenseCategory`, persisting immediately, deletes moving expenses to "Other") — the Profile is a second access point, so there is **no** separate per-trip activities list (a mid-build reversal of the first activities-chip design). The headline shows as a subtitle under the trip header; creating a new trip/dream lands on `?tab=profile`. Manual only (no AI reads it yet — that's Slice 3); no realtime on profile fields. Spec: `docs/superpowers/specs/2026-07-07-trip-profile-tab-slice1-design.md`. Plan: `docs/superpowers/plans/2026-07-07-trip-profile-tab-slice1.md`. Migration applied; verified in-app 2026-07-07.

**Shareable trips: shipped 2026-06-25.** One-toggle public, read-only itinerary sharing. A trip gains `share_token` / `is_public` / `shared_at` (migration `20260625000001_shareable_trip.sql`) and a single read-only `security definer` projection RPC `shared_trip(token)` that returns only the itinerary skeleton (name, country, day_count, locations, days with ordinal/title/tag/tone/location_name/events) — never dates, budget, expenses, savings, or members. Public route `/t/[token]` (`src/app/t/[token]/page.tsx`) renders it read-only with "Day NN" labels for anyone, logged in or not; `proxy.ts` allow-lists the `/t/` prefix so logged-out visitors aren't bounced to `/signin`. Owner side: a `// share` dialog in the trip header (`src/components/share-trip-dialog.tsx`) toggles `shareTrip`/`unshareTrip` and shows the copy link. Copy side: a sticky CTA (`src/app/t/[token]/copy-cta.tsx`) -> start-date prompt -> `copySharedTrip` clones the projection into a new dated trip in the caller's workspace via normal-RLS inserts (no privileged write) — members + default categories + locations (spans derived from days) + days on consecutive dates. New `shared-trip-types.ts` / `shared-trip-queries.ts`. **Deferred:** trip notes in the projection, a discovery/browse UI, and hiding the global Assistant launcher on `/t/` pages (lives in `assistant.tsx`). Spec: `docs/superpowers/specs/2026-06-25-shareable-trip-design.md`. Plan: `docs/superpowers/plans/2026-06-25-shareable-trip.md`. **Migration applied 2026-06-25** — one shared Supabase project (local dev and Vercel prod use the same database), so it is live everywhere; there is no separate prod paste.

**Manual page: shipped 2026-06-25.** A built-in help/reference page reached from the nav alongside Home / Checklists. New `/manual` route (`src/app/manual/page.tsx`) mirrors the Checklists page shell (auth + workspace guard, `LeftRail` + `MobileHeaderNav`, `current="manual"`); a `ManualContent` server component (`src/app/manual/manual-content.tsx`) holds the copy: an overview ("the main manual") plus four orientation-depth sections of the core trip workspace — Trips & Dreams (`#trips`), Itinerary (`#itinerary`), Packing (`#packing`), Budget & Expenses (`#budget`), each anchored for future deep-links. Each section is a client `Section` (`src/app/manual/manual-section.tsx`, `"use client"`) with a `more`/`less` toggle that expands a numbered step-by-step walkthrough (passed as a static `details` JSX prop, server-rendered; `Steps`/`Step` helpers); sections open independently. Nav wiring in `app-nav.tsx`: `"manual"` added to `NavKey`, `buildNavDestinations` (after Checklists, desktop rail on every page), and `MOBILE_NAV_ORDER` (so it rides the mobile arrow cycle home -> trip -> on-the-road -> manual). Presentation only — no DB, migration, deps, or interactivity. Deferred: Home/On-the-road/Checklists/setup/Assistant sections, per-section header deep-links, search. Spec: `docs/superpowers/specs/2026-06-25-manual-design.md`.

**Merged "ask" + "AI mode" into one Assistant: shipped 2026-06-25.** The separate global bottom-left AI on/off pill (`AiFloatingToggle`) and the trip-only bottom-right "ask" button (`TripChat`) became one floating **`Assistant`** (`src/components/assistant.tsx`), mounted once in the root layout. Its launcher opens the mock chat; the panel header now carries the existing `AiToggle` switch + close. Global + chat-always-works: chat opens on every app page (hidden on `/`, `/signin`, `/signup`) regardless of AI on/off, and the toggle only gates the proactive surfaces (`AiSuggestion`, budget drafter) which still read `useAiMode()`. Removed `AiFloatingToggle` (and its `AI_HIDDEN_PATHS` / `usePathname`) from `ai-mode.tsx` (kept `AiModeProvider` / `useAiMode` / `AiToggle`); deleted `trips/[slug]/layout.tsx` and `trips/[slug]/trip-chat.tsx` (the root mount makes chat persist across all navigation). `/profile` AI toggle row unchanged. Mock only. Spec: `docs/superpowers/specs/2026-06-25-assistant-merge-design.md`.

**Mobile nav moved into each page header: shipped 2026-06-24.** Retired the sticky `MobileTopNav` bar; the prev/next arrows now live inside each page's header. `MobileTopNav` -> non-sticky `MobileHeaderNav` (same arrow logic, plus an optional `center` slot and `className`), rendered mobile-only (`lg:hidden`) at the top of each page's header on Home, On the road, Checklists (overview + detail), and the Trip header. The trip header's old `[<- back][Trip 02 of 03][// edit trip]` row became `[<- Home] [// edit trip] [On the move ->]` (count dropped, edit-trip moved to the center slot; `TripHeaderView` gained a `destinations` prop). Sign-out stays in the row on every page. Desktop `LeftRail` and `buildNavDestinations` untouched. Pure presentation. Spec: `docs/superpowers/specs/2026-06-24-mobile-header-nav-design.md`.

**Checklists nav -> desktop only: shipped 2026-06-24.** Checklists dropped out of the mobile arrow nav (it stays on the desktop `LeftRail`): editing checklists is a desktop task, and mobile consumes them via packing's **Import items -> From a checklist**. `NAV_ORDER` renamed `MOBILE_NAV_ORDER = home -> trip -> on-the-road` in `app-nav.tsx`. Two new `MobileTopNav` cases handled: `current="checklists"` (not in the mobile order, e.g. `/checklists` opened by URL on a phone) shows a single `<- Home` back arrow; a one-page nav (only Home, no trip / not on the road) shows no arrows, just sign-out. The bar shell + arrows were factored into `MobileNavBar` / `PrevArrow` / `NextArrow`. The `/checklists` routes are unchanged (still reachable by URL on mobile). Amends the arrow-nav design. Spec: `docs/superpowers/specs/2026-06-24-checklists-desktop-only-nav-design.md`.

**Mobile top nav -> prev/next arrows: shipped 2026-06-24.** `MobileTopNav` (`src/components/app-nav.tsx`) replaced its row of pill chips with two arrows over a fixed page order (`NAV_ORDER = home -> trip -> on-the-road -> checklists`): left arrow = previous page, right arrow = next page, wrapping around so every page is reachable by tapping through. Only present destinations participate; with just two pages (the Home + Checklists minimum) both resolve to the same other page, so a single right arrow shows and the left side is empty. The `trip` label keeps its serif-italic treatment; sign-out stays in the bar (mobile's only sign-out path). Desktop `LeftRail` and `buildNavDestinations` untouched. Pure presentation, no data/query change. Spec: `docs/superpowers/specs/2026-06-24-mobile-arrow-nav-design.md`.

**Budget on the itinerary spine (Slice 1): shipped 2026-06-17.** Planned budget is now woven into the itinerary: each location has a tap-to-expand `BudgetScopeEditor` (category · subject · amount, dates inherited from the location), a Trip-wide section at the foot for place-less costs (with their own start/optional-end dates via new `when_start`/`when_end` columns, migration `20260617000002_budget_item_dates.sql`), and a Planned total. Scoped `saveBudgetItemsForScope` replaces one location (or the trip-wide set) and recomputes `planned_budget_cents`. Always available (not AI-gated). The AI-off `BudgetItemList` was removed from the Budget tab (drafter stays for AI-on). First slice of the planning-spine vision. Spec: `docs/superpowers/specs/2026-06-17-budget-on-itinerary-slice1-design.md`. Plan: `docs/superpowers/plans/2026-06-17-budget-on-itinerary-slice1.md`. **Migration pasted to dev; prod pending.**

**Server-backed budget line items: shipped 2026-06-17.** Budget items (hotels/activities/prices) moved from device localStorage to a shared `trip_budget_items` table (RLS via `is_trip_workspace_member`; migration `20260617000001_trip_budget_items.sql`). New `BudgetItem` types, `getBudgetItems` query, and a replace-all `saveBudgetItems` action that keeps `trips.planned_budget_cents = sum(items)`. AI-off shows an editable `BudgetItemList` (grouped by category, optional place, live total) — replacing the interim `BudgetTotalField`; the AI drafter now loads/saves the same table (localStorage dropped). Both modes share one list. Spec: `docs/superpowers/specs/2026-06-17-budget-items-server-design.md`. Plan: `docs/superpowers/plans/2026-06-17-budget-items-server.md`. **Migration must be pasted into the Supabase SQL editor (dev first; prod pending).**

**AI mode on/off toggle: shipped 2026-06-17.** A per-person switch (off by default) — an always-visible floating pill (`AiFloatingToggle`, fixed bottom-left, mounted in the root layout so it shows on every page) plus a row on /profile — gates every AI surface — suggestion cards (all 7), the trip chat, and the budget assistant. Cookie-based (`ai`, like theme/timezone): server `isAiEnabled()` (`src/lib/ai/ai-mode.ts`) seeds an `AiModeProvider` (`src/components/ai-mode.tsx`) in the root layout; `AiSuggestion`, `TripChat`, and the budget tab self-gate via `useAiMode()`. When off, the budget tab shows a manual `BudgetTotalField` instead of the drafter so budgeting still works. Suggest-only invariant recorded: code under `lib/ai` never writes. No schema, no deps. Spec: `docs/superpowers/specs/2026-06-16-ai-mode-toggle-design.md`. Plan: `docs/superpowers/plans/2026-06-17-ai-mode-toggle.md`.

**Local "today" via device timezone: shipped 2026-06-16.** Replaced UTC-derived `today` (`new Date().toISOString().slice(0,10)`) across trip bucketing (`list-queries.ts`), during-trip itinerary mode (`page.tsx`), the home today signal (`home/page.tsx`), on-the-road (`on-the-road/page.tsx`), and stamped settlement/expense dates (`actions.ts`, `log-expense-row.tsx`). A `TimezoneCookie` client component (mounted in the root layout) writes the device IANA zone to a `tz` cookie and `router.refresh()`es on change; server `localToday()` (`src/lib/time/local-today.ts`) reads it and computes the date via `Intl`, falling back to UTC for the single cold-load render. Pure `todayInTimeZone`/`deviceToday` live in `src/lib/time/today.ts`. No schema, no deps. Resolves the carried "today is UTC-derived" item. Spec: `docs/superpowers/specs/2026-06-16-local-today-timezone-design.md`. Plan: `docs/superpowers/plans/2026-06-16-local-today-timezone.md`.

**Itinerary assistant moved under `+ location`: shipped 2026-06-16 (#56).** The itinerary planning assistant (`<AiSuggestion surface="itinerary" />`) was at the top of the itinerary tab; moved into the planning block, directly below the `+ location` control. Because the planning block only renders for non-active trips, the assistant no longer shows on the itinerary tab during a live trip — that's intentional: the during-trip assistant already lives on the **On the road** page (`surface="road"`), so the two surfaces are cleanly split (planning vs. on the move). Pure presentation; no data/query change.

**Budget by-location empty state simplified: shipped 2026-06-16 (#57).** When a trip has no itinerary locations, `budget-by-location.tsx` now always shows "Add locations in the itinerary to budget by place" instead of conditionally surfacing the unassigned-spend total in that slot. Pure presentation.

**Saved — per-member boxes log contributions (cross-credit + confirm): shipped 2026-06-16.** The two per-member boxes on the Budget **Saved** view were display-only; now the **whole card is a tap target** that logs a savings contribution credited to that member, the amount turning into an inline `+€ … add` field (extracted into a `MemberSavedBox` component in `budget-figures.tsx`). Crediting **your own** box commits immediately; crediting **your partner's** box asks for a confirm first ("Add €X to {name}?" → confirm / cancel), gated on a new `currentUserId` threaded `BudgetTab → SavedFigure → SavingsDetails`. `addSavingsContribution` gained an optional `userId` (defaults to the caller). DB: migration `20260616000002_savings_insert_any_member.sql` relaxes the `savings_insert` RLS `with check` from `user_id = auth.uid()` to "`user_id` is any workspace member of the trip" so either partner can credit either box; delete policy already allowed any member. **Migration must be pasted into the Supabase SQL editor** (applied to dev 2026-06-16; not yet in prod).

**Budget assistant — location-aware seeding (Part A): shipped 2026-06-16.** The assistant is now a hybrid: **Accommodation** and **Activities** steps group by itinerary location — one sub-group per place (header `place · dates`), pre-seeded (one suggested hotel per place; activities start empty), with a "+ add hotel/activity here" so multiple hotels/activities per place work. **Transport / Food / Other** stay flat trip-wide lists. Restores the location-by-location smoothness without losing category structure. The seam `planBudgetSteps` gained an optional `groups: BudgetGroup[]` (vs flat `seed`) and a `locations` input; the drafter keys rows per **bucket** (`step.key` or `step.key:locationId`), and localStorage restores rows into their place (a removed location's saved rows are ignored). No-location trips fall back to a single trip-named group. Still total-only mock. Spec: `docs/superpowers/specs/2026-06-16-budget-location-hybrid-design.md`. Plan: `docs/superpowers/plans/2026-06-16-budget-location-seeding.md`.

**Suggestion-card dedupe: shipped 2026-06-16.** The mock-suggestions work left two cards on several tabs because the trip-level `<AiSuggestion surface="trip" />` in `page.tsx` rendered on *every* tab, on top of each tab's own card (and the original static Packing/Itinerary cards). Fixed: removed the trip-level card (dropped the `"trip"` surface); retired the original static `SuggestionCard`s in Packing and Itinerary, replacing them with surface-specific `<AiSuggestion surface="packing" />` / `"itinerary"`. Now exactly one mock suggestion per surface (budget/packing/itinerary/notes/home/road/checklists), each specific to its tab. `SuggestionCard` stays (rendered by `AiSuggestion`).

**"Activities" expense category added: shipped 2026-06-16.** Added `Activities` to the default expense categories (`EXPENSE_CATEGORIES` → Food / Transportation / Accommodation / Activities / Other) so activity spend has a real bucket and the budget assistant's Activities planning lines up with logged categories. New trips seed it via `createTrip`; existing trips backfilled by idempotent migration `20260616000001_expense_category_activities.sql` (appends Activities at each trip's max sort_order + 1, `on conflict do nothing`). First half of the budget-assistant revisit; the location-aware-seeding hybrid (Part A) follows. Spec: `docs/superpowers/specs/2026-06-16-budget-location-hybrid-design.md`. **Migration must be pasted into the Supabase SQL editor.**

**Mock AI suggestions across surfaces: shipped 2026-06-15.** The moss-bordered `SuggestionCard` (previously a static stub in Packing/Itinerary) now appears, mock-driven, on the Budget view, the trip page (trip-level, below the tabs, all tabs), Notes, Home, On the road, and Checklists. A pure seam `src/lib/ai/suggestions.ts` (`suggestionFor(surface, context?)`) returns a canned plausible `Suggestion` per surface; a client wrapper `src/components/ai-suggestion.tsx` renders it with a working **dismiss** (hides for the session, returns on reload) and an **inert apply**. `SuggestionCard` gained optional `onApply`/`onDismiss` (backward-compatible — the existing Packing/Itinerary cards pass none and stay inert). Real model later = make `suggestionFor` async and generate from the LLM client; `context` reserved for trip facts. **Deferred:** real model, working apply, persisted/shared dismissal, trip-aware context; existing Packing/Itinerary cards left as-is. Spec: `docs/superpowers/specs/2026-06-15-mock-suggestions-design.md`. Plan: `docs/superpowers/plans/2026-06-15-mock-suggestions.md`.

**Trip chat (mock): shipped 2026-06-15.** An open chat across the trip workspace, built mock-first. A floating **ask** button (bottom-right on every `/trips/[slug]` tab) opens a panel with a message list + input; replies come from `src/lib/ai/chat.ts` (`requestChatReply`, a deterministic honest-placeholder that never pretends to be a real answer, after a ~0.6s typing delay). Mounted via a new `src/app/trips/[slug]/layout.tsx` so the conversation survives `?tab=` tab switches. History is ephemeral (kept across tabs, cleared on reload). Wiring a real model later is one function: turn `requestChatReply` into a `fetch('/api/chat', …)` (or have a route call the SDK); a reserved `context` param carries trip facts. **Deferred:** real LLM (provider/key/route/cost), trip-aware context, saved/shared history. Spec: `docs/superpowers/specs/2026-06-15-trip-chat-design.md`. Plan: `docs/superpowers/plans/2026-06-15-trip-chat.md`.

**Budget tab — purpose-first reorder: shipped 2026-06-16.** Each view now leads with its primary action and pushes the assistant down. **Budget** view: Log-an-expense → Spent figure → Compact settle → Budget drafter (Edit budget) → AI suggestion → By-location → Ledger — the assistant card sits directly below Edit budget, with By-location under the assistant (was: spent → settle → suggestion → drafter → by-location → log → ledger). **Expense** view: Log-an-expense → spent → settle → Ledger (was: spent → settle → log → ledger). **Saved** view already purpose-first (inline `+` add at top of `SavedFigure`); **Settle up** untouched. Pure sibling reorder in `budget-tab.tsx`; `LogExpenseRow`'s own `border-t` gives a clean rule under the header. No data/query/component change.

**Budget assistant — Delete budget: shipped then removed 2026-06-16.** Briefly added a **Delete** link (inline confirm, `clearSavedItems` + `updateTripBudget(0)`) to the collapsed drafter row, then removed it the same day: three reset-ish controls (Edit budget / Start over / Delete) was clutter for a rarely-used action, and you can already zero a budget by editing the total to 0. Row is back to **Edit budget** + **Start over** only; `deleteBudget` / `confirmingDelete` / `clearSavedItems` deleted.

**Budget assistant — Start over: shipped 2026-06-16.** The collapsed budget-drafter row now pairs **Edit budget** (left) with a dashed **Start over** pill (far right, shown only when `plannedBudgetCents > 0`). `open()` gained a `fromScratch` flag: Edit budget loads saved localStorage rows as before; Start over calls `open(true)`, skipping `loadSavedItems` so every step re-seeds from the current itinerary (locations, nights, date labels) via `planBudgetSteps` and begins at step 1. Non-destructive — saved rows are only overwritten on Apply. No server/query change.

**Budget assistant — Edit budget + Unassigned row removed: shipped 2026-06-15.** The "Plan a budget" button now reads **Edit budget** once a budget is set (`plannedBudgetCents > 0`), and the plan's line items are saved per trip in localStorage on Apply, so reopening restores your entries to edit (device-local for now; a shared store is deferred). Separately, the **Unassigned** row in Budget-by-location was removed (`UnassignedRow` deleted). No server data/query change.

**Settle up — per-member boxes reflect settlements: shipped 2026-06-15.** The two paid-boxes on the Settle up view showed only expense spend, ignoring settlements. Each box now adds a `settled €X` line when that member sent a settlement and a `received €X` line when the other member did, from the already-computed `summary.settlementsByUser`. Pure presentation — no data/query change.

**Phase 5 (Slice 1) — Guided budget assistant (mock): shipped 2026-06-15.** First concrete Phase 5 piece, built mock-first (no Anthropic SDK, no API calls, no new tables). Instead of a one-shot number, it's a short **category-centric** guided interview on the Budget (planning) side. A pure seam `src/lib/ai/budget-planner.ts` exposes `planBudgetSteps(input)` -> `BudgetStep[]` (five category steps: Accommodation, Transport, Food, Activities, Anything else) and `estimateItemCents()`. Each step is a header + an **add-list of detailed items**, every item being `subject / when / cost` (e.g. "Sea hotel · 3 days · 350"). Accommodation/transport/food come with a seeded suggestion (from nights/members); activities + other start empty. A `BudgetDrafter` client component renders a **Plan a budget** button (shown for any trip with a duration signal; hidden only for a bare dateless dream), walks the steps (back/next), and ends in an editable summary with a live total; **apply** writes only the master budget via the existing `updateTripBudget`. Blank-cost rows get an assistant estimate on advance; an explicit 0 (friends' place, borrowed car) is kept. Total only — no per-category/per-item persistence, no planned-vs-actual (that's stats, not planning). Wiring real Claude later is one file: make `planBudgetSteps`/`estimateItemCents` async and generate from the LLM client; call sites add `await`. **Deferred:** reading logged expenses / itinerary text to seed items (where Claude gets smarter), a real date picker for `when`, and planned-vs-actual analytics. Spec: `docs/superpowers/specs/2026-06-15-budget-ai-drafter-design.md`. Plan: `docs/superpowers/plans/2026-06-15-budget-ai-drafter.md`.

**Later (needs real Claude):** Import an externally-authored plan (paste or upload a trip plan written by ChatGPT/another LLM) and have an agent adapt it into this app's itinerary + budget. Reuses the `lib/ai` seam — the pasted plan flows in through the reserved `context` channel; no new architecture, just the real model behind it. Deferred until Claude is wired.

**Settle-up bar always-on with settle/partial pills: shipped 2026-06-15.** Follow-up to the budget pills work below. The **Settle up** pill now renders the same compact bar as Budget/Expense instead of the old `SettleUpCard`: `settle up` + `partial` pills on the left, a directional status (`you pay` / `you're owed` / `all square`) + amount on the right. `CompactSettle` gained an `alwaysShow` flag so the bar persists at €0 on the Settle up tab (Budget/Expense still hide it when square); `SettleUpCard` retired. Pure presentation. PR #44.

**Budget tab: Budget / Expense / Saved / Settle up pills + compact settle-up: shipped 2026-06-15.** Two rounds. First, the budget tab adopted packing's segmented look: a pill bar in the dusk-tint header switches views instead of one long scroll. Packing's `SegBtn` was lifted into the shared `together` library with a `tone` prop (`clay`/`sea`); `BudgetFigures` split into `SpentFigure` + `SavedFigure`; `BudgetTab` became a client component holding the `view` state. Then the `Budget` pill was split into **Budget** (planning: spent/planned figure + bar + budget-by-location) and **Expense** (tracking: a spent-total header + log expense + ledger). A **compact settle strip** (`settle` + `partial` buttons, plus your `you pay` / `you're owed €X`) now appears at the top of both Budget and Expense, hidden when all square; it reuses a `SettleUpButtons` component extracted from `SettleUpCard`. The **Settle up** pill keeps the full card + paid-by split and gained a **settlement history** list derived from `is_settlement` expense rows (newest first). `Saved` unchanged. Then the dusk-tint header was reduced to just the label + pills bar (pills bar "on its own"); each view's figure (spent / spent-total / saved) now renders as the first block of its content rather than inside the header. Lastly, add-expense + ledger were also added to the **Budget** pill (now in both Budget and Expense, by request). All pure presentation — no data, query, or migration changes. Specs: `docs/superpowers/specs/2026-06-15-budget-segmented-pills-design.md`, `docs/superpowers/specs/2026-06-15-budget-expense-split-design.md`, `docs/superpowers/specs/2026-06-15-budget-pills-own-bar-design.md`, `docs/superpowers/specs/2026-06-15-budget-pill-add-expense-ledger-design.md`. Plans: `docs/superpowers/plans/2026-06-15-budget-segmented-pills.md`, `docs/superpowers/plans/2026-06-15-budget-expense-split.md`, `docs/superpowers/plans/2026-06-15-budget-pills-own-bar.md`, `docs/superpowers/plans/2026-06-15-budget-pill-add-expense-ledger.md`.

**On the Road page (`/on-the-road`) + shared persistent nav: shipped 2026-06-10.** A third top-level surface that wakes only during an active ("now") trip and concentrates the day: a location/weather header (day N / total), today's plan (title + `daySummary` + next/last event via the reused `TodayNextEvent`), a quick add-expense (amount + category + name, saved with `day_date = today`, paid by the current user) with a today's-spend total, a quick note jot (new nullable `trip_notes.day_date` column), and a "Looking ahead" panel (tomorrow's first timed event + next location move, collapsing when they coincide). The route self-guards (redirects to `/home` when no trip's dates contain today). The trip page's `DesktopLeftRail` was extracted into a shared `LeftRail` + a new `MobileTopNav` (`src/components/app-nav.tsx`) and shown on Home / On the road / Trip so navigation is always available — desktop rail, mobile slim top bar, destinations Home / On the road (active-only) / current trip with the active one highlighted. Spec: `docs/superpowers/specs/2026-06-10-on-the-road-page-design.md`. Plan: `docs/superpowers/plans/2026-06-10-on-the-road-page.md`. **Migration `20260610000004_trip_note_day.sql` applied 2026-06-11.**

**Checklists: shipped 2026-06-10.** A workspace-level top-level feature for reusable, resettable checklist templates (trek, camping, surfing…), deliberately a twin of trip packing. Three tables (`checklists`, `checklist_categories`, `checklist_items`) mirror the packing shape but are scoped to the workspace via a new `is_checklist_workspace_member` RLS helper; `checklist_items` carry the same `category` name + `label` + `done` shape as `packing_items` so a future "copy into a trip's packing" stays additive. `/checklists` overview lists saved templates with progress + add/delete; `/checklists/[slug]` is the detail page (categories + items + checkboxes mirroring `PackingTab`, plus rename, a **Reset** that unchecks everything to reuse, and delete). Realtime on `checklist_items` for live check sync. Slug auto-derived from the name and deduped within the workspace. Checklists is a permanent nav destination (left rail + mobile top bar). New `src/lib/checklists/{types,queries,actions}.ts`. Deferred: drag-reorder, seeded templates (empty start), and the trip-packing copy bridge. Spec: `docs/superpowers/specs/2026-06-10-checklists-design.md`. Plan: `docs/superpowers/plans/2026-06-10-checklists.md`. **Migration `20260610000005_checklists.sql` applied 2026-06-11.**

**Phase 4.6 — Itinerary Editing (dated trips): code shipped 2026-05-28 (pending Supabase migration paste to enable Realtime).** The itinerary timeline is now fully editable inline: `+ add day` opens a five-field form (date / tag / title / sub / tone pill picker) with the date pre-filled to the next available slot; `✎` on a day card swaps it for the same form pre-populated; `×` deletes with native confirm. Three Server Actions mirror the `addNote` / `updateNote` / `deleteNote` shape; `23505` collisions on `(trip_id, day_date)` translate to "Another day already uses that date." Realtime channel on `itinerary_days` keeps the two devices in sync. Dreams unchanged — Slice B (numbered days for dreams) and Slice C (drag-to-reschedule) carried below. **Migration applied + verified in-app 2026-05-30** (`20260528000004_phase_4_6_itinerary_realtime.sql`).

**Phase 4.5 — Trip Notes: shipped 2026-05-28.** New Notes tab on `/trips/[slug]` lets any workspace member jot, edit, and delete free-text notes for a trip. Backed by a `trip_notes` table (FK cascade, RLS via `is_trip_workspace_member`). Three Server Actions (`addNote` / `updateNote` / `deleteNote`). Per-trip-only scope; workspace-level `/notes` deferred per the spec.

**Phase 4 — Dream-Trip Pipeline + Edit Trip: shipped 2026-05-28.** Dreams and trips live in one `trips` table distinguished by whether dates are set. `/home` is a real query (Hero / Trips / Dreams / Past bands). `+ new trip` form has a "this is a dream" toggle with a free-text `When?` field. `/trips/[slug]` renders a dream variant when dates are NULL. `/trips/[slug]/edit` lets a workspace member rename, edit, promote/demote, or delete a trip. Six slices, eight commits.

## Phase 4 — Dream-Trip Pipeline
- [x] **1. Schema + dream seeds** — Done 2026-05-28. `20260528000001_phase_4_dreams.sql` adds `trips.fuzzy_when text` and tightens the date-order CHECK from `(end_date is null or start_date is null or end_date >= start_date)` to `(both null) or (both set and end >= start)` — a row is now unambiguously either a dated trip or a dateless dream. `20260528000002_seed_dreams.sql` inserts Faroe Islands / Patagonia / Hokkaido / Aeolian Isles as dream rows (`fuzzy_when='someday'`) for every workspace that has Lombok seeded; idempotent via `on conflict (workspace_id, slug) do nothing`.
- [x] **2. Query layer + state derivation** — Done 2026-05-28. New `src/lib/trips/list-queries.ts` owns `TripState = "now" | "upcoming" | "past" | "dream"`, `TripBuckets`, and `listTripsForWorkspace(workspaceId)`. Pure `deriveState(today, start, end)` does the bucketing — ISO yyyy-mm-dd string compare matches date order so no `Date` round-trip is needed. `TripHeader` gained `fuzzyWhen` and `getTripBySlug` reads the new column. New `src/lib/trips/slug-tone.ts` (`slugToTone`) deterministically maps a slug to one of `sea/clay/moss/sand` — same slug always same tone, no schema column needed, Lombok special-cased to `sea` to preserve the existing visual.
- [x] **3. `/home` rebuild** — Done 2026-05-28. Drops the hardcoded `<Link href="/trips/lombok">` card and the `DREAM_BOARD` array. New `src/app/home/trip-cards.tsx` ships `HeroCard` / `CompactRow` / `DreamTile`. New `src/app/home/format-helpers.ts` does `daysUntil` + `dayWithinTrip` ("day 3 / 8"). Hero claim rule: earliest `now` wins, else soonest `upcoming`. Four bands render conditionally — empty bands omit themselves. Past trips render dimmed (`opacity-60`). The `+ new trip` CTA below Past renames to `+ new trip or dream`.
- [x] **4. `+ new trip` dream toggle** — Done 2026-05-28. `NewTripForm` gains a checkbox at top: when ON, the date fields hide and a free-text `When?` input appears (max 64 chars). Submit label flips between `create trip` and `save dream`. `createTrip` Server Action branches on `isDream`: dreams must have null dates + optional `fuzzy_when`; trips must have both dates set + `fuzzy_when` null. Mixed states are rejected ("Dreams have no dates." / "Trips don't have a 'when?' label.") so the invariant stays clean even if the client form is bypassed.
- [x] **5. `/trips/[slug]` dream variant** — Done 2026-05-28. When `header.startDate === null`, three branches kick in: hero shows the uppercase `fuzzyWhen` (fallback `"SOMEDAY"`) in place of the date range and labels itself "Dream"; the mobile weather strip + desktop right-rail weather grid hide (no dates = no forecast slot); the itinerary tab renders a `DreamItineraryStub` ("No days planned yet — add dates to plan day-by-day") instead of the empty `TabStub`. Packing + budget tabs remain fully functional for dreams — you can plan packing lists and budgets without firm dates.
- [x] **6. Edit trip / promote dream** — Done 2026-05-28. New `/trips/[slug]/edit` route with `EditTripForm` duplicated from `NewTripForm` — visible slug field (no auto-derive), all baseline fields editable, native `confirm()`-gated delete at bottom. Two new Server Actions in `src/lib/trips/actions.ts`: `updateTrip` (mirrors `createTrip` validation so the dream/trip invariant holds on edits too; surfaces `23505` slug collisions as a friendly error; revalidates `/home` + old trip path; returns the new slug so the client can route post-rename) and `deleteTrip` (cascades via existing FKs; server-side `redirect('/home')`). Promotion is implicit: uncheck `is a dream`, fill dates, save. Demotion is symmetric. Entry-point is a small `// edit trip` link in the trip hero (mobile: inline with the `Trip · NN of NN` label; desktop: right-aligned in the lg label row). Spec: `docs/superpowers/specs/2026-05-28-phase-4-edit-trip-design.md`. Plan: `docs/superpowers/plans/2026-05-28-phase-4-edit-trip.md`.

## Phase 4.5 — Trip Notes
- [x] **1. Trip notes** — Done 2026-05-28. New `/trips/[slug]?tab=notes` Notes tab backed by `trip_notes (id, trip_id, body, created_by, created_at, updated_at)` with FK cascade + RLS via `is_trip_workspace_member`. Three Server Actions: `addNote` (validates, inserts, returns the full `TripNote` for optional optimistic prepend), `updateNote` (body-only edit, bumps `updated_at` explicitly), `deleteNote` (form-action throws-on-error pattern, no cascade concerns). `NotesTab` client component: always-visible textarea + `+ save` at top, list of notes newest-first below. Each note shows body (whitespace-preserved), author avatar from the existing `memberToneMap`, ISO date in mono, and `✎` + `×` affordances. Edit-in-place uses a lifted `editingId` state with `NoteView`/`NoteEditor` split components so the editor's local state resets naturally on mount (sidesteps the React 19 `set-state-in-effect` lint). Native `confirm()` gates delete. Per-trip-only scope; workspace-level `/notes` route deferred. No categories, no day association, no Realtime. Spec: `docs/superpowers/specs/2026-05-28-phase-4-5-trip-notes-design.md`. Plan: `docs/superpowers/plans/2026-05-28-phase-4-5-trip-notes.md`.

### Carried into the next Phase 4.5 slice (post-trip)
- **Workspace-level `/notes` route.** The design handoff has `Notes` in the top-level desktop nav. Revisit if "general restaurant ideas not tied to any trip" becomes a felt gap during the Lombok trip.
- **Categories / tags** (restaurant / lodging / tip / idea). Only if browsing notes by type becomes painful.
- **Day association** (`day_date` nullable column + day picker on form). Only if "morning of day 3" notes feel essential.
- **Realtime channel for notes.** Only if simultaneous co-typing becomes a real scenario.

## Phase 4.6 — Itinerary Editing (dated trips)
- [x] **1. Editable itinerary timeline** — Done 2026-05-28. New `ItineraryTab` (`"use client"`) replaces the previous read-only `ItineraryView` on `/trips/[slug]?tab=itinerary`. Inline add (dashed `+ add day` expands to a five-field form), click-to-edit per card (`✎`), native-confirm delete (`×`). Five editable fields: `day_date` (date picker, auto-defaulted to max+1 or trip start), `tag` (free-text, mono-uppercased visually), `title`, `sub`, `tone` (four-pill picker over `ITINERARY_TONES`). Three Server Actions in `actions.ts`: `addItineraryDay` (returns inserted row as `ItineraryDay`), `updateItineraryDay` (validation mirrors add; `23505` collision translated to "Another day already uses that date."), `deleteItineraryDay` (throws-on-error form-action shape). Types + pure helpers (`ITINERARY_TONES`, `ItineraryDay`, `rowToItineraryDay`, `withOrdinals`) split into `src/lib/trips/itinerary-types.ts` so the client bundle doesn't pull `next/headers` via `itinerary-queries.ts` — mirrors the `packing-types.ts` split. Realtime channel on `itinerary_days` for cross-device sync (matches packing pattern — itinerary is synchronous-collaborative). Optimistic state shape lifts from `PackingTab`. `DayCard` splits into `DayView` / `DayEditor` to sidestep React 19's `set-state-in-effect` lint (per `memory/feedback-react19-lint-gotchas.md`). Empty state ("No days planned yet — add the first one.") still renders the add row, so first-day-on-an-empty-trip is one click. Dreams unchanged (`DreamItineraryStub` still renders for dateless trips — Slice B's job). Date-range validation deliberately skipped. Spec: `docs/superpowers/specs/2026-05-28-phase-4-6-itinerary-editing-design.md`. Plan: `docs/superpowers/plans/2026-05-28-phase-4-6-itinerary-editing.md`.

### Carried into the next slices (post-trip)
- [x] **Slice B — Itinerary for dreams.** Done 2026-05-30. Numbered days (1, 2, 3…) for dateless dreams via a parallel `dream_itinerary_days` table keyed on `(trip_id, day_index)` — dated `itinerary_days` untouched. New `dream-itinerary-types.ts` (`DreamDay` / `rowToDreamDay` / `withDreamOrdinals`) + `dream-itinerary-queries.ts` + four Server Actions (`addDreamItineraryDay` / `updateDreamItineraryDay` / `deleteDreamItineraryDay` / `rescheduleDreamItineraryDays`). `DreamItineraryTab` mirrors `ItineraryTab` minus the calendar (no date field, `DAY / 01` left column, no weather/suggestion card); drag-to-reorder permutes `day_index` via the deferred-unique `reschedule_dream_itinerary_days` RPC. Realtime channel on `dream_itinerary_days`. Spec + plan under `docs/superpowers/`. **Migration applied + verified in-app 2026-05-30** (`20260530000001_slice_b_dream_itinerary.sql`).
- [x] **Slice B.2 — Promotion converts dream days to dated days.** Done 2026-05-30. Promoting a dream that has planned days (uncheck "is a dream" + enter a Start in `/trips/[slug]/edit`) moves its `dream_itinerary_days` rows onto consecutive `itinerary_days` (day_index order → Start, Start+1, …), auto-derives `end_date = Start + (count − 1)`, clears `fuzzy_when`, and deletes the dream rows — atomically via the `promote_dream_to_dated(p_trip_id, p_start_date)` RPC (SECURITY INVOKER; converted rows stamped `created_by = auth.uid()` to satisfy itinerary insert RLS). `updateTrip` gained a `wasDream` input + a promotion-with-days branch (non-date fields updated first so slug collisions fail before conversion). The edit form hides the End field and shows a derived preview ("N planned days → Jun 12–Jun 16") when promoting a dream-with-days; zero-day dreams promote via the normal both-dates flow. Spec + plan under `docs/superpowers/`. **Migration applied + verified in-app 2026-05-30** (`20260530000002_promote_dream_to_dated.sql`).
- [x] **Slice C — Drag to reschedule.** Done 2026-05-29. Insertion-shift: dragging a day card resequences activities across the trip's fixed date slots (dates stay the same set; only `day_date` reassigns by position). `@dnd-kit` grip handle on each day card (view-mode only), optimistic `arrayMove` + `rowToItineraryDay`/`withOrdinals` rebuild, revert on error. DB: `unique (trip_id, day_date)` made `DEFERRABLE INITIALLY IMMEDIATE` + `reschedule_itinerary_days(trip_id, day_ids[])` RPC permutes dates in one deferred UPDATE. Action `rescheduleItineraryDays` mirrors `reorderPackingCategories`. Existing Realtime channel syncs the partner. Dated trips only (dreams keep the stub — Slice B still open). Spec + plan under `docs/superpowers/`. **Migration applied + verified in-app 2026-05-30** (`20260529000002_itinerary_reschedule.sql`).
- [x] **Day mini-events (time + text).** Done 2026-06-10. A dated itinerary day's single free-text `sub` line is replaced by an ordered list of mini-events, each an optional time + a text line. New `events jsonb` column on `itinerary_days` (default `[]`); old `sub` backfilled into a single timeless event and left vestigial (non-destructive). New `ItineraryEvent` type + `parseEvents` in `itinerary-types.ts`; `addItineraryDay` / `updateItineraryDay` / `insertItineraryDayWithShift` take `events` and trim/drop empty-text rows; the `shift_and_insert_itinerary` RPC param `p_sub text` became `p_events jsonb`. `DayForm` gains an Events section (per-row time input + text input + `×` remove) and a footer `+ add event` button on the left of `cancel` / `save`. Events **auto-sort by time** (untimed last); typing `11` in a time normalizes to `11:00` on blur. Day card renders the sorted list (time in mono before text). Dream itinerary unchanged. Spec: `docs/superpowers/specs/2026-06-10-itinerary-day-events-design.md`. Plan: `docs/superpowers/plans/2026-06-10-itinerary-day-events.md`. **Migration applied + verified in-app 2026-06-10** (`20260610000001_itinerary_day_events.sql`).
- [x] **Itinerary summary/detail cards + during-trip mode.** Done 2026-06-10. Two phases. **Phase A:** revived the day `sub` as a one-line **Summary** (form field above Events; `daySummary` falls back to the first event / "N events" when blank). Day cards now collapse to that summary and **tap to expand** the full events (`expandedDays` set + `DayView` two-state). Re-added `p_sub` to the `shift_and_insert_itinerary` RPC (`20260610000002_itinerary_shift_sub.sql`). **Phase B (during-trip emphasis, dated trips):** the tab takes a server `today` + `tripEndDate`; `tripActive` / `groupZone` zone days/locations. When today is in range: a top **Past bar** folds everything before today (completed locations + loose past days, counted in days) collapsed by default; the **current location** stays whole with today's day auto-expanded and its own past days folded into an inner "N earlier days" bar; future locations collapse; past items dim (`opacity-60`); the `+ day / + location` planning block recedes below, muted. Outside an active trip it's the normal planning view. Expand/collapse is client-only, re-derived from `today` each load (lazy `useState` initializers, no persistence). Itinerary-only pilot; home/packing/budget deferred. Spec: `docs/superpowers/specs/2026-06-10-itinerary-trip-mode-design.md`. Plan: `docs/superpowers/plans/2026-06-10-itinerary-trip-mode.md`. **Migration applied + verified in-app 2026-06-10.**
- **Date-range validation for itinerary days** (must fall within trip `start_date`…`end_date`). One-line addition to `addItineraryDay` / `updateItineraryDay` validation when typos start hurting.
- [x] **Home hero surfaces the active trip's today.** Done 2026-06-10. Follow-on from the during-trip pilot: when the hero trip is `now`, its footer shows today's day **title** + **summary** (`daySummary` moved to `itinerary-types.ts`, server-rendered) and a **next/last event** line — the next upcoming timed event by the browser clock, falling back to the last once they've passed (`TodayNextEvent`, a `"use client"` component using `useSyncExternalStore` so local time is correct and there's no SSR hydration mismatch / no `set-state-in-effect`). New `getTodayForTrip(tripId, today)` query; `home/page.tsx` fetches it only for an active hero. Hero card only; no ticker; no schema. Spec: `docs/superpowers/specs/2026-06-10-home-today-signal-design.md`. Plan: `docs/superpowers/plans/2026-06-10-home-today-signal.md`.

## Phase 3.5 — Basic CRUD (do one at a time)
- [x] **1. `+ add packing item`** — Done 2026-05-27. New Server Action `addPackingItem(tripId, category, label)` in `src/lib/trips/actions.ts` — trims label, reads `auth.uid()` for `added_by`, lets RLS gate the insert. Inline `AddItemRow` in `packing-tab.tsx` swaps the stub button for: text input + `add` submit + `×` cancel. Submit clears the input but keeps the form expanded so the user can rattle off several items in a row; Esc collapses; disabled state on empty / pending. Both clients pick up the INSERT via the existing Realtime handler (with id-dedupe on echo). Build clean.
- [x] **2. `+ log expense` (form)** — Done 2026-05-27. New Server Action `logExpense(input)` in `src/lib/trips/actions.ts` — trims title, parses amount→cents, validates category against `EXPENSE_CATEGORIES`, inserts non-settlement row, `revalidatePath`s the trip page. New `"use client"` component `LogExpenseRow` (`src/app/trips/[slug]/log-expense-row.tsx`) replaces the stub `LogExpenseCta` footer in `budget-tab.tsx`: inline-expanding form with title, amount, day-of-trip select, category select, and a two-pill paid-by toggle (degrading to `<select>` for >2-member workspaces). Submit clears title+amount but keeps category/paid/day for batch entry; Esc collapses. Server Action's `revalidatePath` updates the ledger and total in-place; no Realtime channel — `RefreshOnVisible` covers the cross-device case. `CATEGORY_TONE` extended with `Lodging` (sand) and `Other` (ink). Build + lint clean.
- [x] **2b. Expense date: free picker, defaults to today** — Done 2026-06-03. Replaced the trip-days `<select>` ("Day") in `expense-fields.tsx` with a native `<input type="date">` ("Date") on both the add (`LogExpenseRow`) and inline-edit (`LedgerRowEditor`) forms. Adding an expense now defaults the date to today regardless of trip range (was: today only if within range, else trip start). Dropped the `dayOptions`/`enumerateDays`/`DayOption` plumbing end-to-end (`budget-tab` → `log-expense-row`/`ledger-row` → `expense-fields`) and the now-unused `startDate`/`endDate` props on `BudgetTab`; removed the dead `enumerateDays`/`DayOption` exports from `expense-types.ts`. Ledger date display (`ledgerDate`) unchanged. Build + lint clean.
- [x] **3. `+ new trip` (form)** — Done 2026-05-28. New Server Action `createTrip(input)` in `src/lib/trips/actions.ts` validates name/slug/dates/coords (slug regex, end ≥ start, lat/lng pair + ranges), inserts a `trips` row + N `trip_members` rows for every workspace member, and returns `{ slug }` on success (or `{ error }` with friendly text on validation or `23505` slug collision). New `/trips/new` route (`src/app/trips/new/page.tsx`) hosts a `"use client"` `NewTripForm` component: name, slug (auto-derived from name via `src/lib/trips/slugify.ts`, with a `slugDirty` flag stopping the auto-derive on first manual edit), start/end dates (native pickers), country, and a `› advanced` disclosure for lat/lng. On success the client `router.push`es to `/trips/<slug>`. The dashed `+ new trip` button on `/home` is now a `<Link>` to the route — same classes, byte-identical visual. `budget-tab.tsx` gained a graceful-empty branch: when `plannedBudgetCents === 0` it hides the planned-cap `<Bar>`, the `/ €0.00` text, and the `"X% of planned"` line. Lombok unaffected (still €2,800 from fixtures). Known gap: `/home` is still hardcoded to the Lombok card and won't show the new trip — verified manually by navigating to `/trips/<slug>` directly. Build + lint clean. **Phase 3.5 complete.**

### Post-Phase-3 polish (shipped between phases)
- **2026-06-11 — Sign-in lands on the active trip.** A plain sign-in (default `/home` destination) now routes to `/on-the-road` when a trip is active right now, else `/home`. `defaultLanding()` in `src/lib/auth/actions.ts` mirrors the `/on-the-road` guard (`getCurrentWorkspace` → `buckets.now[0]`); an explicit `?next=` deep link is still honored. Verified in-app: signing out and back in during an active trip lands on on-the-road. Confirms the freshly-set session cookie is readable within the same server-action request.
- **2026-05-27 — Realtime focus-refetch.** Mobile browsers suspend backgrounded WebSocket connections, so partner's Realtime events were being missed on real-phone testing. New `src/components/refresh-on-visible.tsx` (client) calls `router.refresh()` on `visibilitychange` + `focus`; mounted on `src/app/trips/[slug]/page.tsx`. `packing-tab.tsx` adds a `useEffect` to sync the refreshed `initialItems` prop into its local state. Build clean. See DECISIONS row for the belt-and-suspenders rationale.

## Next small tasks (do one at a time)
1. ~~Initialize Next.js project here with TypeScript + Tailwind~~ **Done 2026-05-25** — Next 16.2.6, React 19.2, Tailwind v4, pnpm, App Router, `src/`, Turbopack. `pnpm dev` → http://localhost:3000 returns 200.
2. ~~Add Shadcn/ui and install a few base components (button, input, dialog)~~ **Done 2026-05-25** — shadcn 4.8 with `base-nova` style and `neutral` base color. Built on `@base-ui/react`. `button`, `input`, `dialog` installed. `cn()` helper at `src/lib/utils.ts`. Theme tokens in `src/app/globals.css`. `pnpm build` clean.
3. ~~Create Supabase project, add `.env.local` with URL + anon key~~ **Done 2026-05-25** — project ref `zctbypyfvebhildcdkto`, URL + publishable/anon key in `.env.local` (gitignored). `.env.example` committed as template. Connectivity verified via `GET /auth/v1/settings` → HTTP 200.
4. ~~Wire `@supabase/ssr` client (browser + server)~~ **Done 2026-05-25** — `@supabase/ssr` 0.10 + `@supabase/supabase-js` 2.106 installed. Clients at `src/lib/supabase/{client,server}.ts`. Session refresh in `src/proxy.ts` (Next 16's renamed middleware convention; Node runtime). Verified: `proxy.ts` runs on `GET /` in dev (proxy timing visible in Next log), build clean, no deprecation warnings.
5. ~~Build minimal landing page in the project's warm style. Swap globals.css palette from neutral to warm pink/teal/off-white per DESIGN.md.~~ **Done 2026-05-25** — editorial-style landing at `/` with Instrument Serif + Hanken Grotesk + JetBrains Mono via `next/font/google`. Full warm palette in `globals.css` (OKLCH tokens, light + dark). Soft peach/teal radial-gradient atmosphere. Staggered fade-in on load. Mobile-first, tested at 390px and 1440px. Prod build clean.
6. ~~Push to GitHub, connect Vercel, confirm deploy works~~ **Done 2026-05-25** — pushed to `MonKeyP95/project-template-couples`, Vercel project connected with `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` env vars. Production live at https://project-template-couples.vercel.app — HTTP 200, full visual match, proxy + Supabase env vars confirmed working in prod.

## Phase 1 — Foundation: COMPLETE
All 6 tasks done. Next is **Phase 2 — Auth + Pairing**.

## Phase 2 — Auth + Pairing: COMPLETE 2026-05-26
- ~~Email/password sign-up + log in (Supabase Auth)~~ Done.
- ~~`workspaces` table with members + roles (RLS on)~~ Done.
- ~~Invite flow: one member sends a link, the other joins the workspace~~ Done — single-use 14-day shareable link.
- ~~Basic profile (name, avatar)~~ Done — name editable; avatar is initials only (image upload deferred).

Implementation followed `docs/superpowers/specs/2026-05-25-phase-2-auth-pairing-design.md` and `docs/superpowers/plans/2026-05-25-phase-2-auth-pairing.md`. Local E2E verified: signup → invite → partner signup via invite → both see each other on `/home` → profile edit → sign-out → proxy redirect → inline error on wrong password.

## Phase 3 — First Trip (after Phase 2 works)
Working from `design_handoff_together_app/README.md` (hi-fi handoff for Phase 3 + parts of Phase 4). Suggested order in that file's "Suggested implementation order" section.

- [x] **1. Token + font migration** — Done 2026-05-26. `src/app/globals.css` rebased to sand-and-sea (`:root` + `.dark` + extended `sea/sand/clay/moss/dusk` + tints exposed via `@theme inline`), shadow tokens, type recipes (`t-display/t-label/t-mono/t-num`). Pink primary replaced with deep sea — existing `text-primary` italic words now render sea-blue automatically. Fonts: IBM Plex Sans + IBM Plex Mono + Instrument Serif. `pnpm build` clean.
- [x] **2. Primitives under `src/components/together/`** — Done 2026-05-26. Eight presentational Server Components: `Label`, `Coord`, `Avatar`, `PairAvatar`, `MonoBadge`, `Bar`, `Chevron`, `TopoBg`. All use the extended-palette Tailwind classes (`bg-sea`, `border-clay`, etc.) from step 1 — no inline `var(--…)` styles for palette tokens. `PairAvatar` uses `-ml-1.5` for the −6px overlap. `TopoBg` strips colons from `useId()` so the SVG `<pattern>` id is a valid URL fragment. Three ink levels in the prototype collapsed to `foreground` + `muted-foreground` (no third tone yet). Barrel export at `src/components/together/index.ts`. `pnpm build` + `pnpm lint` clean for new files.
- [x] **3. Redesign `/home` mobile-first** — Done 2026-05-26. New `/home` matches Screen 1 of the handoff: `Together · Workspace` Label + paired-avatar header, dynamic `MM / DD · Weekday` date label, 58px serif greeting with member display name italic, hairline rule, member sub-row with `est. YYYY · N members` (year from `workspaces.created_at`, now exposed by `getCurrentWorkspace`), Upcoming trip card (Lombok fixture, links to `/trips/lombok` which 404s until step 4), Dream board 2×2 grid (Faroe / Patagonia / Hokkaido / Aeolian Isles with per-pin tone tints), dashed `+ new trip` CTA, subtle mono `Sign out` footer (preserved from prior page since the design didn't address it). `InviteCard` swaps in for the Upcoming section when `youOnly`. Orphaned `src/components/initials-avatar.tsx` deleted. Page capped at `max-w-[440px] mx-auto` so it doesn't sprawl on desktop until step 9. `/signin` + `/signup` visually verified at 390px viewport — pink is gone everywhere, italic words now read sea-blue.
- [x] **4. `/trips/[slug]` route with static Lombok fixture** — Done 2026-05-26. New dynamic route at `src/app/trips/[slug]/page.tsx`; `notFound()` for any slug other than `lombok`. Hero header (sea-tint, `TopoBg` at 18%, 64px serif `Lombok`, `WaveGlyph`), 7-cell weather strip, itinerary timeline (8 days with per-tone left border + `MonoBadge`). Tab switching via `?tab=itinerary|packing|budget` searchparam — Packing/Budget render small "Arriving soon" stubs until steps 6/7 land. Fixed-position pill nav at the viewport bottom, scoped to the same 440px column as the page. Two new primitives shipped: `DayChip` and `WaveGlyph`. `MonoBadgeTone` extended with `"sand"` so ARRIVE/DEPART read warm instead of falling through to gray (prototype quirk corrected). New `--sea-2` token (oklch(0.55 0.06 205) light / oklch(0.62 0.06 210) dark) added for the haze weather glyph. Fixture lives at `src/lib/trips/fixtures.ts` — step 5 will swap it for a Supabase query. Route returns 307 → signin when unauthed.
- [x] **5. `trips` + `trip_members` migrations with RLS; trip route reads live** — Done 2026-05-26 (code). Schema in `supabase/migrations/20260526000001_phase_3_trips.sql`: `trips (id, workspace_id, slug, name, country, start_date, end_date, lat, lng, created_by, created_at)` with unique `(workspace_id, slug)` + date-order check; `trip_members (trip_id, user_id, role, added_at)`. RLS on both: reuses `is_workspace_member()` for `trips`, adds `is_trip_workspace_member()` for `trip_members`. Seed in `20260526000002_seed_lombok.sql` — idempotent `do $$` block that picks the oldest workspace + member, inserts Lombok, and adds all workspace members to `trip_members`. New `src/lib/trips/queries.ts` with `getTripBySlug(workspaceId, slug)` returning a `TripHeader` (+ `index/total` derived by counting workspace trips). `fixtures.ts` refactored down to itinerary + weather only (`TripDetail` shape, picked up by slug). The route now composes the two and formats `coord` from lat/lng and `dateRange` from dates. **User action required**: paste the two SQL files into the Supabase SQL Editor in order — until then `/trips/lombok` will 404 because no row exists.
- [x] **6. `packing_items` migration + Packing tab with Server Action toggle + Realtime** — Done 2026-05-26 (code). Schema `supabase/migrations/20260526000003_phase_3_packing.sql`: `packing_items (id, trip_id, category, label, done, added_by, created_at)` indexed on `(trip_id, created_at)`. RLS reuses `is_trip_workspace_member()`. Table added to `supabase_realtime` publication. Seed `20260526000004_seed_lombok_packing.sql` inserts the 17-item fixture (5 categories), idempotent, all `added_by` = workspace owner. New `CheckRow` primitive with `pop` + `checked-line` keyframes added to `globals.css`. New Server Action `togglePackingItem(id, done)` at `src/lib/trips/actions.ts`. Live query at `src/lib/trips/packing-queries.ts`; shared types + `groupPackingItems()` split into `packing-types.ts` so the client bundle doesn't pull the server Supabase client. Client component `src/app/trips/[slug]/packing-tab.tsx`: `useState` + manual optimistic toggle (revert on action error) + Supabase Realtime channel filtered on `trip_id` so the partner's checks sync without refresh. Trip page fetches packing data only when `?tab=packing`. **User action required**: paste both SQL files into the Supabase SQL Editor in order.
- [x] **7. `expenses` migration + Budget tab + settle-up Server Action** — Done 2026-05-27. Schema `supabase/migrations/20260527000001_phase_3_expenses.sql`: `expenses (id, trip_id, title, amount_cents, currency default 'EUR', paid_by, category, day_date, is_settlement, created_at)`, RLS via `is_trip_workspace_member()`. Insert policy widened so `paid_by` can be any workspace member (settle action records on behalf of debtor). Seed `20260527000002_seed_lombok_expenses.sql` inserts the 7-row design fixture; workspace owner ≡ Monkey, other member ≡ Giraf. `expense-types.ts`: `Expense` + `summarizeBudget()` computing `(aPaid-bPaid)/2 + aTransfers - bTransfers` so settlements net out. `expense-queries.ts` for the live ledger. Server Action `settleUp(tripId, slug)` appended to `actions.ts` — throws on error (form-compatible) and `revalidatePath`s. New `src/app/trips/[slug]/budget-tab.tsx`: Server Component with dusk-tint header, settle-up card wired to `<form action={settleUp.bind(...)}>`, 2-col split breakdown, ledger with date + category MonoBadge + payer avatar, `+ log expense` stub. Trip page fetches budget data only when `?tab=budget`. Planned budget (€2,800) hardcoded in `fixtures.ts` until the trips schema gets a `planned_budget_cents` column. **User action required**: paste both SQL files into the Supabase SQL Editor in order.
- [x] **8. `itinerary_days` migration + live timeline** — Done 2026-05-27 (code). Schema `supabase/migrations/20260527000003_phase_3_itinerary.sql`: `itinerary_days (id, trip_id, day_date, title, sub, tag, tone, created_by, created_at)`, `unique (trip_id, day_date)`, indexed on `(trip_id, day_date)`. Tone constrained to `sea/clay/moss/sand`. RLS reuses `is_trip_workspace_member()`. Seed `20260527000004_seed_lombok_itinerary.sql` inserts the 8 design-fixture rows (Jun 12–19), idempotent. New `src/lib/trips/itinerary-queries.ts` owns the `ItineraryDay` type and `getItineraryDays(tripId)` — query selects `day_date/title/sub/tag/tone` ordered ascending, then derives `d` (ordinal, padded), `dow` (3-char weekday, UTC), `date` ("Jun 12", UTC) at the query layer so the view stays unchanged. `fixtures.ts` slimmed to weather + `plannedBudgetCents`; `ItineraryDay` import on the page moved to the new module. Itinerary fetched lazily (only when `activeTab === 'itinerary'`) to mirror packing/budget. Empty-itinerary case falls through to the existing "Arriving soon" stub. `pnpm build` clean. **User action required**: paste both SQL files into the Supabase SQL Editor in order — until then `/trips/lombok?tab=itinerary` will show the "Arriving soon" stub instead of the timeline.
- [x] **9. Desktop breakpoint pass** — Done 2026-05-27. `/home` and `/trips/[slug]` lift the 440px cap at md/lg. `/home` at `md:` (768+): header on right, greeting on left, stat row (Upcoming / Dream places / Members + est. year), trip grid `md:grid-cols-2 lg:grid-cols-3`, dream board `md:grid-cols-4` (aspect-[4/5] at md). `/trips/[slug]` at `lg:` (1024+): 3-col flex — left rail (220px, workspace label + Home/Trip nav + members list), wide center (hero scaled to 88px serif, inline tabs with counts), right rail (280px, pre-trip Packing + Budget progress bars, 7-day weather grid). BottomNav hidden at `lg:`; mobile weather strip hidden at `lg:` and moved into right rail. Trip page now always loads packing + expenses at the page level so the right rail can show counts on every tab (previously loaded per-tab). New helpers in-file: `DesktopLeftRail`, `DesktopRightRail`, `DesktopTabs`, `ProgressRow`, `StatItem`. Verified visually at 390 / 768 / 1280 / 1440 against the production build (dev server in a broken Jest-worker state — pre-existing, unrelated). Mobile layouts untouched.
- [x] **10. Pre-AI: moss-bordered suggestion card stub** — Done 2026-05-27. New primitive `SuggestionCard` at `src/components/together/suggestion-card.tsx` (Server Component): moss left border, moss `/ label`, body slot for mixed roman + italic copy, optional chevron-down (`expandable`), optional `applyLabel` / `dismissLabel` rendering inert mono uppercase buttons. Barrel export updated. Packing tab's previously-inlined card swapped for `<SuggestionCard label="/ suggested for Rinjani" expandable>`. Itinerary view gains a second instance below the timeline (`/ assistant` + apply/dismiss) matching the desktop dashboard pattern. Buttons are non-functional by design — Phase 5 will wire copy + actions through `lib/ai/claude.ts`. `pnpm build` clean.

## Backlog (post-Lombok-trip, not yet phased)

Items surfaced by the 2026-05-28 doc audit — referenced in `VISION.md` / `FEATURES.md` / `PLAN.md` / `design_handoff_together_app/README.md` but not in any phase. Pick up after the Lombok trip when real-use signal tells us which actually matter. Trip notes are intentionally **not** listed here — they're scheduled as Phase 4.5.

### Maps / visualization
- **Visited-countries map.** Color in each country a couple has taken a trip to. Not a tweak: the current `WorldMapBg` is anonymous land outlines (`fill:none`, no per-country paths), so it needs a country-keyed map asset (static SVG with ISO `id`s, or `react-simple-maps` + a world atlas) plus a `country` text → ISO-code lookup (the `trips.country` field is free text). Decide scope first — colored map behind everything is busy once countries light up; a dedicated "places you've been" map on `/home` is likely the better home. Listed under FEATURES "Later".

### Profile + workspace
- **Profile avatar uploads.** Phase 2 shipped with `avatar is initials only (image upload deferred)`; `PLAN.md:22` lists it as a Phase 4 candidate. Shape: Supabase Storage bucket (`avatars/<user_id>.jpg`), `<input type="file">` on `/profile`, server action uploads + writes `profiles.avatar_url`. The existing `Avatar` primitive in `src/components/together/` swaps from initials to an `<img>` when `avatar_url` is set; `PairAvatar` follows automatically.

### Per-item edit/delete (deferred-in-spec, never tracked)
The edit-trip spec (`docs/superpowers/specs/2026-05-28-phase-4-edit-trip-design.md`) explicitly deferred these:
- [x] **Packing categories (first-class, draggable).** Done 2026-05-29. New `packing_categories` table (id, trip_id, name, sort_order) + RLS + idempotent backfill (`20260529000001_packing_categories.sql`). Items still link by `category` text (no rename = no drift). `PackingTab` renders all categories incl. empty in registry order; add-category control, smart-confirm delete (cascades items), and drag-to-reorder via `@dnd-kit` (grip handle, `touch-none` + 8px activation for mobile). Category structure syncs via `revalidatePath` + the existing `RefreshOnVisible`; item checks keep their live Realtime channel. Three Server Actions: `addPackingCategory` / `deletePackingCategory` / `reorderPackingCategories`. `groupPackingItems(categories, items)` drives render order and appends orphan groups (race guard). Spec + plan under `docs/superpowers/`. **Migration applied + verified in-app 2026-05-30** (`20260529000001_packing_categories.sql`).
- [x] **Edit / delete packing items.** Done 2026-05-29. Two new Server Actions in `actions.ts` (`updatePackingItem`, `deletePackingItem`), both return-`{error}` shaped like `togglePackingItem` — no `revalidatePath`, the already-mounted Realtime channel broadcasts the UPDATE/DELETE to both clients. `PackingTab` gained an `editingId` state plus optimistic `update`/`remove` handlers mirroring the existing `toggle`. Each row split into `ItemRow` (view: `CheckRow` + `✎`/`×`) and `ItemEditor` (inline text field, save/cancel, Esc-to-close) — a split component so its local input state seeds from props on mount, sidestepping the React 19 set-state-in-effect lint (per `memory/feedback-react19-lint-gotchas.md`). Native `confirm()` gates delete. Build + lint clean.
- [x] **Edit / delete expenses.** Done 2026-05-30. Two new Server Actions in `actions.ts`: `updateExpense` (validation mirrors `logExpense`; never touches `is_settlement`) and `deleteExpense` (regular or settlement; returns `{error}`). Both `revalidatePath` only — no Realtime, matching the budget tab. The ledger row moved out of `budget-tab.tsx` into a `"use client"` `ledger-row.tsx` split into `LedgerRow` (state holder) / `LedgerRowView` (adds `✎` + `×`) / `LedgerRowEditor` (inline form, seeds from props on mount per the React 19 set-state-in-effect lint). The five form fields (title / amount / day / category / paid-by) were extracted from `log-expense-row.tsx` into a shared controlled `ExpenseFields` (`expense-fields.tsx`, also exports `enumerateDays`); `LogExpenseRow` now consumes it so add + edit stay in visual lockstep. **Settlement rows are delete-only** — `✎` is hidden on them (no editable fields), `×` doubles as undo-settle-up. Delete is `confirm()`-gated with an `opacity-50` pending state (no optimistic local removal; `revalidatePath` drops the row). `CATEGORY_TONE` / `ledgerDate` moved alongside the row into `ledger-row.tsx`. Build + lint clean.

### Theming
- [x] **Dark mode toggle.** Done 2026-05-30. Investigated first: there was **no** `prefers-color-scheme` media query and no `.light` class — dark mode is class-based only (`@custom-variant dark (&:is(.dark *))` + a complete `.dark {}` block), so the app was always light until now. Implemented **cookie-based SSR theming** (binary light/dark, light default): new `src/lib/theme.ts` (`isDarkTheme()` reads the `theme` cookie); `RootLayout` is now `async`, appends `dark` to the existing `<html>` className when the cookie is set, and gained `suppressHydrationWarning` (added here — it was not present before); `src/components/theme-toggle.tsx` (`"use client"`) flips `.dark` on `<html>` for instant feedback **and** writes the `theme` cookie so the next server render matches (cookie name inlined, not imported from `theme.ts`, to keep `next/headers` out of the client bundle per the client/server split rule); `/profile` renders `<ThemeToggle initialDark={…} />` in an "Appearance" row between the email/member-since list and the "Back to home" link. Added `color-scheme: light/dark` to `:root`/`.dark` so native date pickers, selects, and scrollbars follow the theme. No FOUC, no inline script, no hydration mismatch — verified via curl: `theme=dark` cookie → `<html class="dark">`, absent/`light` → no class. Build + lint clean. **Note**: the public landing `/` stays statically prerendered (light) regardless of cookie; all authenticated routes (`/home`, `/profile`, `/trips/*`, `/signin`) are dynamic and honor it. Deferred: honoring OS `prefers-color-scheme` on first visit, and a left-rail toggle on desktop.
  - **Follow-up 2026-05-30 (desktop left-rail toggle):** added `<ThemeToggle>` to the bottom of the `DesktopLeftRail` on `/trips/[slug]` (lg+), under an "Appearance" label with a top border, so the theme is reachable on trip pages without scrolling to a footer. `page.tsx` computes `isDarkTheme()` once and passes `initialDark` to the rail.
  - **Won't do — OS `prefers-color-scheme` on first visit:** the server can't read the OS preference at request time, so honoring it needs either a blocking inline `<head>` script (rejected in `DECISIONS.md`) or a duplicated `.dark` token block inside an `@media` query (DRY hazard). Both are real complexity for a marginal first-visit nicety; the toggle already lets anyone set + persist their choice. Left deferred/declined intentionally.
  - **Follow-up 2026-05-30:** the toggle was only on `/profile`, which had no inbound link, so it was unreachable. Added `<ThemeToggle>` directly to the `/home` footer (alongside "Sign out") so the theme can be flipped without leaving home. Verified in-browser: flips light/dark and persists. (A brief detour wrapping the home-header avatar in a `/profile` link was reverted once the footer toggle landed.)

## Budget polish — 2026-06-07
- **Ledger now collapsible (collapsed by default).** Extracted the `Ledger` block out of `budget-tab.tsx` into a new client component `budget-ledger.tsx` so `budget-tab` stays a Server Component; the "Ledger · N" header is a toggle button (`aria-expanded`) whose rows render only when open, with a "show" / "most recent" hint.
- **Date label on each location's budget envelope.** `Envelope` carries a `dateLabel` computed in `summarizeEnvelopes` via the pure `locationDateLabel` helper (reuses `formatShortDate`). Mirrors the itinerary header exactly: declared span when both ends set, else the range derived from the location's days; `EnvelopeRow` renders it (e.g. `8 Jun – 12 Jun`) next to the name, blank only when undated.

## Notes tab — Location filing — 2026-06-08
- [x] **Location-filed notes** — Done 2026-06-08. Notes in the trip Notes tab can now be filed under existing itinerary locations. The Notes tab shows a collapsible list: "General" block first (location-less notes), then one block per location in itinerary order. Each block has an in-block `+ add note` form; editing a note has a location select to re-file it. Data: `trip_notes.location_id` references `itinerary_locations(id)` with `on delete set null` (deleting a location turns its notes into General notes). Locations are created only in the Itinerary tab. Migration `20260608000003_trip_note_location.sql` (apply by hand in the Supabase SQL editor).

## Between-phase polish — 2026-05-30 (UI session)
Small UI changes + one bugfix, all verified in-browser (Playwright, light + dark, mobile + desktop):
- **Home "Trips" band → mini-hero cards.** The non-hero upcoming trips rendered as small colorless `CompactRow`s; replaced with a new `TripCard` in `home/trip-cards.tsx` — a shorter hero (tone surface + `TopoBg`, larger italic name, `// now` badge + coord, day-count footer) that drops into the same `md:grid-cols-2 lg:grid-cols-3` grid. Past trips keep the dimmed `CompactRow`.
- **Budget crash fix: `enumerateDays` server/client boundary.** `BudgetTab` (Server Component) called `enumerateDays`, which lived in `expense-fields.tsx` (`"use client"`) after the edit-expense work made the tab compute `dayOptions` server-side. Moved the pure helper + `DayOption` type into `@/lib/trips/expense-types.ts` (boundary-neutral) and repointed `budget-tab` / `log-expense-row` / `ledger-row` / `expense-fields`. Same class of fix as the `packing-types.ts` / `itinerary-types.ts` splits.
- **"+ add expense" moved to the top of the ledger** (was a footer below the rows) — sibling reorder in `budget-tab.tsx`.
- **Label "log expense" → "add expense"** (collapsed row + expanded form header) in `log-expense-row.tsx`, matching the existing submit button.
- **World map behind `/signup`.** New `WorldMapBg` primitive (`src/components/together/world-map-bg.tsx`): 338 land outlines from the public-domain Wikimedia "World map - low resolution" SVG, rendered `fill:none` + `stroke="currentColor"` so it follows the theme. Faint (`text-foreground/[0.07]`) behind the form. See DECISIONS row.
- [x] **Trip/dream editable budget + shared saved-so-far tracker (budget tab inline edit)** — Done 2026-06-01. Added `planned_budget_cents` and `saved_cents` columns to the `trips` table; both editable and shared across the workspace via existing RLS. Budget now editable per trip/dream with inline form in budget tab. Saved amount is a shared running total tracked by the app.
- [x] **Small saved bar on `/home` trip + dream cards** — Done 2026-06-03. `listTripsForWorkspace` now selects `planned_budget_cents` / `saved_cents` onto `TripListItem`. New `SavedBar` helper in `home/trip-cards.tsx` renders a thin moss `Bar` (`savedCents / plannedBudgetCents`) plus a `N% saved` label in `HeroCard`, `TripCard`, and `DreamTile`; hidden until a budget is set, matching the budget tab's rule.
- [x] **Itinerary multi-day add (date range / day count)** — Done 2026-06-03. The Add-day form can now create several days at once. Dated itinerary: the single Date field becomes "From" + an optional "To" date; a later "To" inserts one identical, independent entry per day in the inclusive range. Dream itinerary: a "Days" number field (default 1) appends that many identical entries at the end. Both extend the existing add action (`addItineraryDay` gains `endDate`, `addDreamItineraryDay` gains `count`) with a single bulk `insert([...])` — no migration. Range capped at 31 days; the dated `(trip_id, day_date)` unique makes a range collision all-or-nothing ("Some days in that range are already planned."). Entries are fully independent after creation — no grouping/linking. Edit forms unchanged (still per-day). Spec: `docs/superpowers/specs/2026-06-03-itinerary-multi-day-design.md`. Build + lint clean.
- [x] **Itinerary "added together" grouping (`group_id`)** — Code shipped 2026-06-03 (pending Supabase migration paste). Reverses the multi-day add's "no grouping" stance with a nullable `itinerary_days.group_id` (`20260603000001_itinerary_group_id.sql`): a 2+ day add stamps one shared `crypto.randomUUID()` on its rows; single adds and pre-existing rows stay null. `group_id` threads through `ItineraryRow`/`ItineraryDay` (`groupId`), `rowToItineraryDay`, the query `.select`, and the Realtime row. The dated itinerary brackets maximal runs of consecutive same-`group_id` days in a fine `border-rule` rounded box with an "added together" caption; single days render as before. The line survives per-day edits; moving a day out of the run just breaks contiguity (no id clearing). Dated itinerary only — dreams unchanged. Spec addendum: `docs/superpowers/specs/2026-06-03-itinerary-multi-day-design.md`. Build + lint clean.
- [x] **Itinerary locations — Slice 1** — Shipped 2026-06-03 (PR #5). New `itinerary_locations` table (name + sort_order, RLS + Realtime) + nullable `itinerary_days.location_id` (`20260603000002_itinerary_locations.sql`, `on delete set null`). The dated itinerary is now organized by location: a vertical stack of **collapsible groups** styled like the day cards (ordinal + tone-colored name + day-count/date-range + chevron), day cards nested beneath, ordered top-to-bottom by earliest day. Per-location rename/delete, per-group `+ day`, global `+ location`; unfiled days collect in a trailing "In transit" group. Move a day between locations via the edit form's Location select. The `group_id` trek box nests inside a location. **Drag-to-reschedule removed** (returns in Slice 2 with computed dates). New `location-types.ts` / `location-queries.ts`; four location actions in `actions.ts`. Spec: `docs/superpowers/specs/2026-06-03-itinerary-locations-design.md`; plan: `docs/superpowers/plans/2026-06-03-itinerary-locations.md`. Slices 2 (computed dates + cascade + reorder) and 3 (travel-day polish), and locations-for-dreams, still to come. Build + lint clean.
- [x] **Itinerary block name (`group_name`)** — Done 2026-06-04 (PR #7). Migration applied + verified in-app. The Add-day form can name a multi-day block: when a "To" date is set a "Block name" field appears; the name is stamped on every row of the 2+ day span (single adds and pre-existing rows stay null) via a nullable `itinerary_days.group_name` (`20260603000003_itinerary_group_name.sql`). Rides the same threading path as `group_id` — `ItineraryRow`/`ItineraryDay` (`groupName`), `rowToItineraryDay`, the query/insert `.select`, and the Realtime row. The "added together" caption now prefers the block name (rendered `text-foreground` as a real label vs. muted fallback); display-only, survives per-day edits. Dated itinerary only. Spec: `docs/superpowers/specs/2026-06-03-itinerary-block-name-design.md`; plan: `docs/superpowers/plans/2026-06-03-itinerary-block-name.md`. Build + lint clean.
- [x] **Itinerary delete a multi-day block** — Done 2026-06-04 (PR #8). New `deleteItineraryGroup(tripId, tripSlug, groupId)` Server Action deletes every day sharing a `group_id`, scoped to the trip (mirrors `deleteItineraryDay`). A small `×` on the block caption (top-right of the bracketed box) is a server-action `<form>` with a native confirm ("Delete all N days in this block? This can't be undone."); reuses the existing `revalidatePath` + Realtime DELETE path (Supabase emits one event per row, so the partner's view clears automatically). No migration — reuses `group_id`. Build + lint clean.
- [x] **Itinerary empty slots + click-to-fill (dated-anchors Slice 2, builds 1-2a)** — Done 2026-06-04, verified in-app. The calendar dates *between* a location group's planned day-segments show as faint dashed "empty" cards (date + "empty"), driven entirely by existing `day_date`s — no schema, no DB. Two pure helpers in `itinerary-types.ts` (`gapDates(a,b)` exclusive-range, `formatShortDate`); the `itinerary-tab.tsx` segment map emits an empty slot per gap date before each segment. Slots never render before a group's first day or after its last, and never inside a contiguous `group_id` trek box. **Click-to-fill:** each empty slot is now a button that opens the existing `AddDayRow` pre-filled with that slot's date + the group's location (new `addDayDate` state; `AddDayRow` keyed so it remounts on date change; plain `+ day` resets the seed to the next free day). Filling a free date reuses the unchanged `addItineraryDay`, no shift; the gap closes and the empty card is replaced by the editable day card (deleting reopens the gap). Spec: `docs/superpowers/specs/2026-06-04-itinerary-gap-days-design.md`; plan: `docs/superpowers/plans/2026-06-04-itinerary-empty-slots.md`. Still to come: overflow confirm-and-push (the `shift_itinerary_from` RPC, for adds that land on a taken date) and optional location `start_date`/`end_date` spans (build-3). Build + lint clean.
- [x] **Itinerary overflow confirm-and-push (dated-anchors Slice 2, build-2b)** — Done 2026-06-04, verified in-app. Adding a day/block whose date(s) are already taken no longer errors: `addItineraryDay` now flags its `23505` collision as `dateTaken`, and `AddDayRow.submit` shows a native confirm ("No empty day there — push the following days forward to make room?"). On confirm it calls a thin `insertItineraryDayWithShift` action over the `shift_and_insert_itinerary` RPC (`20260604000001_itinerary_shift_insert.sql`), which inserts the day(s) and pushes the occupied tail forward atomically under the deferrable unique. **Gap-aware:** the push consumes any empty buffer days at the insertion point — it shifts by `count − (free runway before the first occupied day)`, not the full count, so e.g. dropping a 2-day group onto a single empty day pushes the next day by 1 (contiguous, no wasted/trailing empty). Single days and multi-day treks both push (RPC branches on `p_count`); `end_date` extends to still cover the last day, never shrinks. Realtime + revalidate sync the shifted dates to the partner. Spec: `docs/superpowers/specs/2026-06-04-itinerary-gap-days-design.md`; plan: `docs/superpowers/plans/2026-06-04-itinerary-overflow-push.md`. Still to come: optional location `start_date`/`end_date` spans (build-3). Build + lint clean.
- [x] **Itinerary location date spans (dated-anchors Slice 2, build-3)** — Done 2026-06-04, verified in-app. A location can carry an optional date span via nullable `start_date`/`end_date` on `itinerary_locations` (`20260604000002_itinerary_location_dates.sql`, both-or-neither check). The existing **✎** control expands into a name + From/To editor (save/cancel; the old blur-to-close is gone); `renameItineraryLocation` gained `startDate`/`endDate` params. The per-group renderer was rewritten to draw an empty slot for **every** unoccupied date across the group's *effective range* (declared span unioned with its days), interleaved with day-segments in date order — so a dated location shows its whole span as fillable empties (incl. leading/trailing), while an undated location is unchanged (gaps only between days). New `dateRange(a,b)` inclusive helper replaces the `gapDates` usage. Locations now order by `startDate` when set, else earliest day, so a dated-but-empty location sorts into place. The locations Realtime INSERT/UPDATE handlers now route through `rowToLocation` so spans sync live. Scoped to the non-overlapping case; the overlap **confirm-and-push** (a shift-only RPC that also moves later locations) is the final follow-up. Spec: `docs/superpowers/specs/2026-06-04-itinerary-gap-days-design.md`; plan: `docs/superpowers/plans/2026-06-04-itinerary-location-spans.md`. Build + lint clean.
- [x] **Itinerary location overlap confirm-and-push (dated-anchors Slice 2, final)** — Done 2026-06-04, verified in-app. Setting a location's span onto occupied dates no longer silently overlaps: `renameItineraryLocation` (now takes `tripId`) detects an overlap — another location's day/transit day, or another location whose span *starts* inside the window — and returns `needsPush` without writing; the ✎ editor's save shows a native confirm and calls `setLocationSpanWithShift`, a thin wrapper over the `set_location_span_with_shift` RPC (`20260604000003_location_span_shift.sql`). The RPC opens room atomically by shifting everything at/after the span start forward by the **gap-aware overflow** (span length − free runway), moving later days (any location/transit, but **not** this location's own days) and later locations' spans as whole units, then writes the span and extends `end_date`. Locations never overlap (in the realistic later-bump case); a *straddling* earlier span (a start that falls strictly inside another location's span) is **refused** with a message ("That start date falls inside <Location> — set it on or after that location ends."), since pushing can't make room without splitting that location — `renameItineraryLocation` detects it (a `start_date < newStart <= end_date` location) and the ✎ editor surfaces the error inline (PR #13). Realtime: shifted days ride the `itinerary_days` channel, shifted location spans the `itinerary_locations` channel. This completes the dated-anchors feature (empty slots → click-to-fill → day overflow push → location spans → location overlap push). Spec: `docs/superpowers/specs/2026-06-04-itinerary-gap-days-design.md`; plan: `docs/superpowers/plans/2026-06-04-itinerary-location-overlap-push.md`. Build + lint clean.
- [x] **Itinerary day-push keeps location spans in sync** — Done 2026-06-04 (PR #14), verified in-app. Follow-up: the day overflow-push RPC `shift_and_insert_itinerary` (#10) shifted days but left location spans behind, desyncing a dated location's declared span from its (shifted) days. The RPC now maintains spans too (SQL-only; same migration file `20260604000001_itinerary_shift_insert.sql`, `create or replace`): a location entirely at/after the insertion (`id is distinct from p_location_id and start_date >= p_from_date`) moves as a whole unit; the location being added into and any straddler (`id = p_location_id or start_date < p_from_date`, `end_date >= p_from_date`) keeps its start (`least(start, from)`) and extends its end by the gap-aware shift; `trips.end_date` now also covers `max(location end_date)`. Mirrors `set_location_span_with_shift`'s span maintenance.
- [x] **Itinerary loose days + date-led cards** — Done 2026-06-04, verified in-app. No migration. (a) **Date-led card:** derived `dom`/`mon` on `ItineraryDay`; `DayView`'s left rail now leads with the calendar date (`12 / JUN / FRI`) and the corner shows the trip ordinal (`day N`). Dreams (`DreamItineraryTab`) untouched. (b) **Loose days (Scope B):** the "In transit" group is gone — `buildGroups` was replaced by `buildTimeline` returning a date-sorted `TimelineItem[]` of location blocks + bare loose segments (location-less days, interleaved by date); a shared `DaySegmentView` renders a segment (cards or `group_id` trek box) in both contexts. A top-level **"+ day"** (beside "+ location") adds a location-less day. Empties stay location-only. Loose adds reuse `addItineraryDay`/`insertItineraryDayWithShift` with `location_id = null`. **Fix:** `addItineraryDay` now **refuses** adding a day onto a date inside *another* location's span (an empty slot there) — *"That date is inside &lt;Location&gt; — add the day there, or pick another date."* — the target location is excluded so filling its own empties + same-location overflow push still work; this prevents a loose day double-rendering against a location's empty slot. Spec: `docs/superpowers/specs/2026-06-04-itinerary-loose-days-design.md`; plan: `docs/superpowers/plans/2026-06-04-itinerary-loose-days.md`. Build + lint clean.
- [x] **Copy packing + notes from another trip** — Done 2026-06-04, verified in-app. No migration. A one-time **snapshot copy** (not a live link): from the trip you're viewing, pull a packing list or notes from another same-workspace trip/dream. Shared `getImportableTrips(tripId)` read (same-workspace siblings) + a reusable client `ImportFromTripControl` picker (button → `<select>` → copy) mounted on the **Packing** and **Notes** tabs, each wiring its own copy action. `copyPackingFromTrip` merges by category name (skips same-name to respect the unique `(trip_id, name)`; items link by name), copies item labels with `done` reset to unpacked. `copyNotesFromTrip` re-authors each note body to the current user. Both **additive** (nothing deleted), no dedup; app-level actions gated by existing RLS (same-workspace membership). Copied packing items sync via the existing Realtime channel; categories + notes via `revalidatePath` + `RefreshOnVisible`. **Budget copy: won't do** (decided 2026-06-04 — copying the real expense ledger into a new trip is meaningless, and there's no expected-cost template to copy; the `copy*FromTrip` + picker shape could still host it if that ever changes). Spec: `docs/superpowers/specs/2026-06-04-share-packing-notes-design.md`; plan: `docs/superpowers/plans/2026-06-04-share-packing-notes.md`. Build + lint clean.
- [x] **Location-bucketed budget envelopes** — Done 2026-06-06. Per-location budget targets via `itinerary_locations.budget_cents`; expenses attribute to a location by explicit `expenses.location_id` tag, else by date (expense date → itinerary day → location), else Unassigned. The trip's `planned_budget_cents` stays the master; location targets are an allocation of it (allocated vs. unallocated shown in the UI). A "Move budget" action rebalances targets between envelopes or the unallocated pool; going over target shows as "over", never an error. A Location | Month toggle on the budget tab groups spend by month in insight-only mode (no month targets). Build + lint clean.
- [x] **Saved-so-far → per-person contribution log** — Done 2026-06-06. "Saved so far" is no longer a single anonymous `trips.saved_cents` pot; it's a per-person log. New `trip_savings_contributions` table (`user_id` + `amount_cents` + `created_at`, RLS mirrors expenses, self-credited insert via `user_id = auth.uid()`; `20260606000001_savings_contributions.sql`) is the single source of truth — `trips.saved_cents` was **dropped** and the saved total is now `SUM(amount_cents)`, derived everywhere it's read (trip detail via new `getTripSavings`/`summarizeSavings` in `savings-queries.ts`/`savings-types.ts`; home cards via a per-trip sum in `list-queries.ts`; `savedCents` removed from `TripHeader`). Each `+ add` logs one contribution credited to the current user (`addSavingsContribution`); `deleteSavingsContribution` removes one; no in-place edit, no realtime (revalidate like expenses); `updateTripBudget` now handles planned budget only. UI: the "Saved so far" headline + moss bar are visually unchanged, but tapping the saved **number** toggles an inline panel with per-person cards (2-member trips) + a deletable contribution log. Start-fresh — no migration of old amounts. Spec: `docs/superpowers/specs/2026-06-06-savings-contribution-log-design.md`; plan: `docs/superpowers/plans/2026-06-06-savings-contribution-log.md`. Build + lint clean; migration paste + in-app verify pending.
- [x] **Budget-move log + per-location activity** — Done 2026-06-07. Each budget move (cover / leftover transfer) is now recorded in a new `trip_budget_moves` table, written atomically inside the `move_location_budget` RPC; moves appear in the main ledger and under each affected location, and are excluded from the spent total and settle-up. Each envelope (location or Unassigned) is expandable to its own activity: attributed expenses (reusing the editable ledger row) + signed budget moves. Every main-ledger expense row shows an effective-location chip (explicit tag in clay, date-derived in muted, else "unassigned"). Deleted-location move endpoints render "Unallocated" (FK on delete set null). Migration: `20260607000001_budget_moves.sql`. Build + lint clean; migration paste + in-app verify pending.
- [x] **Settle-up: default date + partial/overpay settle** — Done 2026-06-03. (1) `settleUp` now stamps the settlement row's `day_date` with today (`new Date().toISOString().slice(0,10)`, UTC to match `ledgerDate`'s display) instead of leaving it null/"—". (2) New `partialSettleUp(tripId, tripSlug, amount)` Server Action: records a settlement for an arbitrary amount paid by the debtor — paying less leaves a remainder, paying more flips the balance so the other member owes the difference (no cap; only `>0` + int4-overflow guards). Both settle paths now share a private `loadTripBalance(tripId)` helper (the net computation that was inline in `settleUp`) and a `TODAY()` helper. `SettleUpCard` extracted from `budget-tab.tsx` into its own `"use client"` `settle-up-card.tsx`: a "partial" button left of "settle" reveals an inline amount input (`ok` to submit, placeholder hints the owed amount); the card just shows the recomputed remaining (no "gave X" annotation). The settlement still appears as a normal Ledger row. Build + lint clean.

- [x] **Add expense categories (budget add-expense)** — Done 2026-06-08, verified in-app. Expense categories are no longer a hardcoded list; they're per-trip rows in a new `expense_categories` table (mirrors `packing_categories`' shape: name + sort_order, RLS via `is_trip_workspace_member`, expenses link by category-name text). The category control is the **plain native dropdown with a trailing "+ Add category…" entry** (`CategorySelect`): picking it reveals a small inline input that creates and selects the new category. New trips get the 7 defaults (Surf/Dive/Trek/Food/Transit/Lodging/Other) seeded in `createTrip`; existing trips are backfilled by the migration. Categories thread from the loader (`getTripExpenseCategories`) → `BudgetTab` → the add-form + ledger islands as props; `revalidatePath` refreshes the dropdowns after an add (no shared client state). Server-side category validation relaxed to a non-empty check. **Add-only — no delete** (the user iterated through a manage-panel and a pills-with-✕ design, then asked to return to the simple dropdown with add-only; `deleteExpenseCategory` removed). No rename/edit, no reorder, no per-category color (custom categories use the `"ink"` badge tone). Migration: `20260608000002_expense_categories.sql` (applied). Build + lint clean.

- [x] **Styled category/location dropdowns (add-expense)** — Done 2026-06-09. The native `<select>` option lists in the add-expense form (Category, Location) rendered as the browser's plain white menu, off-theme. Replaced with a custom dropdown built on Base UI's `Select` (already a dep) — new `src/components/ui/select.tsx` primitive set (`Select`/`SelectTrigger`/`SelectValue`/`SelectContent`/`SelectItem`) styled to the dialog's popover surface (`bg-popover`, `rounded-xl`, `ring-foreground/10`, clay-tint highlight, clay check indicator, chevron icon); the trigger keeps the existing underline look (`border-b border-rule`, clay focus) so the closed state is visually unchanged. `CategorySelect` and the Location field in `expense-fields.tsx` now use it (the "+ Add category…" entry is a clay-tinted item via the existing sentinel; "Auto (by date)" is a `null`-valued item; the Location trigger maps the id back to its name). `Positioner` uses `alignItemWithTrigger={false}` for a normal below-trigger popover. Note: Base UI's `onValueChange` always passes `Value | null` (clearable), so `CategorySelect.onSelect` now guards null. Also trimmed the seeded-on-create defaults (`EXPENSE_CATEGORIES`) from 7 (Surf/Dive/Trek/Food/Transit/Lodging/Other) to 4 basics — **Food / Transportation / Accommodation / Other** — users add the rest; new badge tones added to `CATEGORY_TONE` (Transportation=sea, Accommodation=sand), old keys kept for already-seeded trips. New trips only; no migration. Build + lint clean.

- [x] **Itinerary collapsible empty-day ranges** — Done 2026-06-09. Inside an open location, a run of 2+ *consecutive* empty days now folds into one dashed range row (`08 Jun – 11 Jun · 4 empty days`) with a `›`/`⌄` chevron, **collapsed by default**; clicking it expands to the per-date empty buttons, which fill exactly as before. Pure rendering in `itinerary-tab.tsx` — no schema/actions/helpers. The existing date-sorted `items` array is coalesced into `rows` (adjacent `empty` items are always calendar-consecutive since any occupied date is a `seg` between them); the inline single-empty button markup was extracted to an `EmptyDayButton` reused by both the length-1 case and the expanded list; expand state lives in an `expandedRuns: Set<string>` keyed `${locationId}:${firstDate}`. Lone empty days are unchanged. Spec: `docs/superpowers/specs/2026-06-09-itinerary-empty-day-ranges-design.md`; plan: `docs/superpowers/plans/2026-06-09-itinerary-empty-day-ranges.md`. Build + lint clean.

- [x] **HeroCard current-weather box** — Done 2026-06-09. The `/home` HeroCard shows a small condition-icon + current temperature top-left (beside the `// now` badge; alone on an upcoming trip), for the trip's destination; coords stay top-right. Data goes through one stubbed file `src/lib/weather/get-weather.ts` (`getWeather(lat, lng): Promise<Weather>`, `Weather = { tempC, code }` with `code` = WMO weather code) returning mock values — `HeroCard` is now an async server component that awaits it when the trip has `lat`/`lng`, else renders no weather. `WeatherBadge` in `trip-cards.tsx` maps the WMO code to a lucide icon (clear/cloudy/rain/snow). **Real weather API deferred** — wiring it later changes only the stub's body, never the UI. Spec: `docs/superpowers/specs/2026-06-09-hero-weather-box-design.md`; plan: `docs/superpowers/plans/2026-06-09-hero-weather-box.md`. Build + lint clean.

- [x] **HeroCard schematic route panel (desktop)** — Done 2026-06-09. On desktop (`md:` and up) a schematic route panel sits beside the hero card at the same footprint — it draws the hero trip's itinerary location names as a deterministic decorative line (no real map tiles, no coordinates). Built from the hero trip's ordered `itinerary_locations` rows (names only). When the trip has no locations a quiet `// map` placeholder renders instead. Desktop-only; mobile layout unchanged.

## UI declutter + sign-out — 2026-06-15
- [x] **Trip tabs: reorder + default to Budget, drop count badges** — Done 2026-06-15 (PR #35, merged). Trip page tab order is now **Budget → Itinerary → Packing → Notes**, with **Budget** the default landing tab (it carries the bare `/trips/[slug]` URL; the other tabs use `?tab=`). Removed the per-tab count badges (`· N days`, `· N`, `· €X`) from the desktop tab bar — the `DesktopTabs` `counts` prop and `labelFor` helper are gone; the underlying counts still feed the right-rail summary cards. `page.tsx` only.
- [x] **Home: remove desktop stats row** — Done 2026-06-15 (PR #35, merged). Dropped the desktop `Upcoming / Dreams / Members / est. year` stat row and its unused `StatItem` component from `/home`. The mobile member-names · est-year line under the divider is unchanged.
- [x] **Sign-out button in nav** — Done 2026-06-15 (PR #36, merged). New `SignOutButton` (`app-nav.tsx`) posts to the existing `/api/signout` route, wired into both `LeftRail` (labeled "Sign out" row, desktop) and `MobileTopNav` (right-aligned icon). Icon from `lucide-react`. No migration.

## Semi-private packing lists — 2026-06-15
- [x] **Three owner-scoped packing views (My / Shared / Partner)** — Done 2026-06-15. The single packing progress bar is replaced by a three-way switcher: **My list** (semi-private), **Shared** (today's list), and **[Partner]'s list**. A nullable `owner_id` on both `packing_items` and `packing_categories` (`null` = shared, a user id = personal) drives everything — the three views are the same two tables filtered by owner (`partitionByOwner` helper). Each scope has its own categories, add rows, and drag-reorder. Privacy is **UI-only**: select RLS stays member-gated (either partner *can* read both lists), insert is tightened to `owner_id is null or owner_id = auth.uid()`; the partner view is rendered **read-only** (no add/edit/delete/drag, non-interactive checkboxes) and gated behind a confirm dialog shown **once per page session**. No per-view done/total counts (dropped). Category uniqueness switched to `unique nulls not distinct (trip_id, owner_id, name)` so each scope gets its own names while shared duplicates still collide. `addPackingItem`/`addPackingCategory` gained an `owner` param; `deletePackingCategory` scopes its item delete by owner; `copyPackingFromTrip` copies the **shared** list only. The right-rail packing count is scoped to shared + the current user (a partner's personal items don't inflate your total). Realtime unchanged (carries `owner_id`). Migration: `20260615000001_packing_owner.sql` (applied). Spec: `docs/superpowers/specs/2026-06-15-semi-private-packing-design.md`; plan: `docs/superpowers/plans/2026-06-15-semi-private-packing.md`. Merged via PR #38. Build + lint clean; verified in-app.
- [x] **Packing tab defaults to My list** — Done 2026-06-15 (PR #39). Opening a trip's Packing tab now lands on **My list** rather than **Shared** (the spec/plan had defaulted to Shared as the familiar view; the user preferred their own list first). One-line change to the initial `view` state in `packing-tab.tsx`.
- [x] **Packing "Import items" dialog (from a trip or a checklist)** — Done 2026-06-15. A single **Import items** button (download icon) sits at the right of the packing switcher row and opens a modal: step 1 picks **From a trip** or **From a checklist**; step 2 has a **To: My/Shared** target toggle (defaults to the active list) plus the source picker — for a trip, a dropdown and a **From: Shared/My** source toggle (default Shared); for a checklist, just a dropdown. Imports are one-time, additive, items arrive unpacked, categories merged by name within the target scope. New `getImportableChecklists` + `copyChecklistToPacking` actions; `copyPackingFromTrip` generalized with `sourceOwner` + `targetOwner` (was shared→shared only). Replaces the in-list "copy packing from another trip" bar; the **Notes** tab's import control is untouched. New self-contained `ImportItemsDialog` (calls the actions directly; body remounts per open so state resets without a reset-effect). No migration; reuses the existing packing Realtime + revalidate. Spec: `docs/superpowers/specs/2026-06-15-add-from-checklist-design.md`; plan: `docs/superpowers/plans/2026-06-15-add-from-checklist.md`. Build + lint clean; in-app verify pending.

## Itinerary end-date floor + auto-grow — 2026-06-15
- [x] **Start-date floor + forward-only `end_date` growth** — Done 2026-06-15 (landed by cherry-picking a long-shelved branch onto current `main`). `addItineraryDay` / `updateItineraryDay` now reject a day dated before the trip's `start_date` (a hard floor via a shared `tripStartDate` helper; dateless dreams are a no-op). `end_date` stays **elastic**: `addItineraryDay` / `updateItineraryDay` / `renameItineraryLocation` grow `trips.end_date` forward through a shared `growTripEndDate` so the header always covers planned content (matching what the push/span RPCs already did). Forward-only — deleting the last days never pulls `end_date` back. One-time idempotent reconcile migration `20260608000001_reconcile_trip_end_dates.sql` repairs trips whose content had already drifted past `end_date`. Build + lint clean; migration paste pending.

## Working rules
- One task per session. Finish, validate, then move on.
- After completing a task, check it off here and add a row to `DECISIONS.md` if anything non-obvious was chosen.
- If a task feels too big mid-session, split it on this page.
